---
version: "2.1"
services:
  web:
    image: nginx
    expose:
      - port: 80
        to:
          - global: true
profiles:
  compute:
    web:
      resources:
        cpu:
          units: "100m"
        memory:
          size: "128Mi"
        storage:
          size: "1Gi"
  placement:
    datacenter:
      attributes:
        region: us-east
        confidential-compute: "true"
        confidential-compute-tee: intel-tdx
      pricing:
        web:
          denom: uact
          amount: 50
deployment:
  web:
    datacenter:
      profile: web
      count: 1
