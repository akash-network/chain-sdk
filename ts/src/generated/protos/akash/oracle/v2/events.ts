import { patched } from "../../../../patches/nodePatchMessage.ts";
import { toTimestamp, fromTimestamp, fromJsonTimestamp, isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: akash/oracle/v2/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp.ts";
import { AggregatedPrice, DataID } from "./prices.ts";

/** EventPriceData is emitted when new price data is added to the oracle */
export interface EventPriceData {
  /** source is the address of the price source (oracle provider) */
  source: string;
  /** id identifies the price pair (denom and base_denom) */
  id:
    | DataID
    | undefined;
  /** price is the decimal price value */
  price: string;
  /** timestamp is when this price was recorded */
  timestamp: Date | undefined;
}

/** EventPriceStaleWarning is emitted when price has not been updated and is about to become stale */
export interface EventPriceStaleWarning {
  /** id identifies the price pair */
  id:
    | DataID
    | undefined;
  /** last_height is the block height when the price was last updated */
  lastHeight: bigint;
  /** blocks_to_stall is the number of blocks until the price becomes stale */
  blocksToStall: bigint;
}

/** EventPriceStaled is emitted when a price has become stale */
export interface EventPriceStaled {
  /** id identifies the price pair */
  id:
    | DataID
    | undefined;
  /** last_height is the block height when the price was last updated before becoming stale */
  lastHeight: bigint;
}

/** EventPriceRecovered is emitted when a stale price has started receiving updates again */
export interface EventPriceRecovered {
  /** id identifies the price pair */
  id:
    | DataID
    | undefined;
  /** height is the block height when the price recovery was detected */
  height: bigint;
}

/** EventAggregatedPrice is emitted when aggregated price has an update */
export interface EventAggregatedPrice {
  /** price is the aggregated price data */
  price: AggregatedPrice | undefined;
}

function createBaseEventPriceData(): EventPriceData {
  return { source: "", id: undefined, price: "", timestamp: undefined };
}

const _EventPriceData: MessageFns<EventPriceData, "akash.oracle.v2.EventPriceData"> = {
  $type: "akash.oracle.v2.EventPriceData" as const,

  encode(message: EventPriceData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.id !== undefined) {
      DataID.encode(message.id, writer.uint32(18).fork()).join();
    }
    if (message.price !== "") {
      writer.uint32(26).string(message.price);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventPriceData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPriceData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = DataID.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.price = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPriceData {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      id: isSet(object.id) ? DataID.fromJSON(object.id) : undefined,
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
    };
  },

  toJSON(message: EventPriceData): unknown {
    const obj: any = {};
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.id !== undefined) {
      obj.id = DataID.toJSON(message.id);
    }
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventPriceData>): EventPriceData {
    const message = createBaseEventPriceData();
    message.source = object.source ?? "";
    message.id = (object.id !== undefined && object.id !== null) ? DataID.fromPartial(object.id) : undefined;
    message.price = object.price ?? "";
    message.timestamp = object.timestamp ?? undefined;
    return message;
  },
};

function createBaseEventPriceStaleWarning(): EventPriceStaleWarning {
  return { id: undefined, lastHeight: 0n, blocksToStall: 0n };
}

export const EventPriceStaleWarning: MessageFns<EventPriceStaleWarning, "akash.oracle.v2.EventPriceStaleWarning"> = {
  $type: "akash.oracle.v2.EventPriceStaleWarning" as const,

  encode(message: EventPriceStaleWarning, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      DataID.encode(message.id, writer.uint32(18).fork()).join();
    }
    if (message.lastHeight !== 0n) {
      if (BigInt.asIntN(64, message.lastHeight) !== message.lastHeight) {
        throw new globalThis.Error("value provided for field message.lastHeight of type int64 too large");
      }
      writer.uint32(24).int64(message.lastHeight);
    }
    if (message.blocksToStall !== 0n) {
      if (BigInt.asIntN(64, message.blocksToStall) !== message.blocksToStall) {
        throw new globalThis.Error("value provided for field message.blocksToStall of type int64 too large");
      }
      writer.uint32(32).int64(message.blocksToStall);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventPriceStaleWarning {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPriceStaleWarning();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = DataID.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.lastHeight = reader.int64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.blocksToStall = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPriceStaleWarning {
    return {
      id: isSet(object.id) ? DataID.fromJSON(object.id) : undefined,
      lastHeight: isSet(object.last_height) ? BigInt(object.last_height) : 0n,
      blocksToStall: isSet(object.blocks_to_stall) ? BigInt(object.blocks_to_stall) : 0n,
    };
  },

  toJSON(message: EventPriceStaleWarning): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = DataID.toJSON(message.id);
    }
    if (message.lastHeight !== 0n) {
      obj.last_height = message.lastHeight.toString();
    }
    if (message.blocksToStall !== 0n) {
      obj.blocks_to_stall = message.blocksToStall.toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventPriceStaleWarning>): EventPriceStaleWarning {
    const message = createBaseEventPriceStaleWarning();
    message.id = (object.id !== undefined && object.id !== null) ? DataID.fromPartial(object.id) : undefined;
    message.lastHeight = (object.lastHeight !== undefined && object.lastHeight !== null) ? BigInt(object.lastHeight) : 0n;
    message.blocksToStall = (object.blocksToStall !== undefined && object.blocksToStall !== null) ? BigInt(object.blocksToStall) : 0n;
    return message;
  },
};

function createBaseEventPriceStaled(): EventPriceStaled {
  return { id: undefined, lastHeight: 0n };
}

export const EventPriceStaled: MessageFns<EventPriceStaled, "akash.oracle.v2.EventPriceStaled"> = {
  $type: "akash.oracle.v2.EventPriceStaled" as const,

  encode(message: EventPriceStaled, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      DataID.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.lastHeight !== 0n) {
      if (BigInt.asIntN(64, message.lastHeight) !== message.lastHeight) {
        throw new globalThis.Error("value provided for field message.lastHeight of type int64 too large");
      }
      writer.uint32(16).int64(message.lastHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventPriceStaled {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPriceStaled();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = DataID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.lastHeight = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPriceStaled {
    return {
      id: isSet(object.id) ? DataID.fromJSON(object.id) : undefined,
      lastHeight: isSet(object.last_height) ? BigInt(object.last_height) : 0n,
    };
  },

  toJSON(message: EventPriceStaled): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = DataID.toJSON(message.id);
    }
    if (message.lastHeight !== 0n) {
      obj.last_height = message.lastHeight.toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventPriceStaled>): EventPriceStaled {
    const message = createBaseEventPriceStaled();
    message.id = (object.id !== undefined && object.id !== null) ? DataID.fromPartial(object.id) : undefined;
    message.lastHeight = (object.lastHeight !== undefined && object.lastHeight !== null) ? BigInt(object.lastHeight) : 0n;
    return message;
  },
};

function createBaseEventPriceRecovered(): EventPriceRecovered {
  return { id: undefined, height: 0n };
}

export const EventPriceRecovered: MessageFns<EventPriceRecovered, "akash.oracle.v2.EventPriceRecovered"> = {
  $type: "akash.oracle.v2.EventPriceRecovered" as const,

  encode(message: EventPriceRecovered, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      DataID.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.height !== 0n) {
      if (BigInt.asIntN(64, message.height) !== message.height) {
        throw new globalThis.Error("value provided for field message.height of type int64 too large");
      }
      writer.uint32(16).int64(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventPriceRecovered {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPriceRecovered();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = DataID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.height = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPriceRecovered {
    return {
      id: isSet(object.id) ? DataID.fromJSON(object.id) : undefined,
      height: isSet(object.height) ? BigInt(object.height) : 0n,
    };
  },

  toJSON(message: EventPriceRecovered): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = DataID.toJSON(message.id);
    }
    if (message.height !== 0n) {
      obj.height = message.height.toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventPriceRecovered>): EventPriceRecovered {
    const message = createBaseEventPriceRecovered();
    message.id = (object.id !== undefined && object.id !== null) ? DataID.fromPartial(object.id) : undefined;
    message.height = (object.height !== undefined && object.height !== null) ? BigInt(object.height) : 0n;
    return message;
  },
};

function createBaseEventAggregatedPrice(): EventAggregatedPrice {
  return { price: undefined };
}

export const EventAggregatedPrice: MessageFns<EventAggregatedPrice, "akash.oracle.v2.EventAggregatedPrice"> = {
  $type: "akash.oracle.v2.EventAggregatedPrice" as const,

  encode(message: EventAggregatedPrice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.price !== undefined) {
      AggregatedPrice.encode(message.price, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAggregatedPrice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAggregatedPrice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.price = AggregatedPrice.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAggregatedPrice {
    return { price: isSet(object.price) ? AggregatedPrice.fromJSON(object.price) : undefined };
  },

  toJSON(message: EventAggregatedPrice): unknown {
    const obj: any = {};
    if (message.price !== undefined) {
      obj.price = AggregatedPrice.toJSON(message.price);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAggregatedPrice>): EventAggregatedPrice {
    const message = createBaseEventAggregatedPrice();
    message.price = (object.price !== undefined && object.price !== null)
      ? AggregatedPrice.fromPartial(object.price)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function _unused_fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function _unused_fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

export const EventPriceData = patched(_EventPriceData);
