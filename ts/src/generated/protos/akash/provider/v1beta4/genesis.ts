import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: akash/provider/v1beta4/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { ProviderMaintenanceRecord } from "./maintenance.ts";
import { ProviderMaintenanceParams } from "./params.ts";
import { Provider, ProviderRegistration } from "./provider.ts";

/** GenesisState defines the basic genesis state used by provider module. */
export interface GenesisState {
  /** Providers is a list of genesis providers. */
  providers: Provider[];
  /** Params is the provider module parameter set. */
  params:
    | ProviderMaintenanceParams
    | undefined;
  /** Maintenances is the list of provider maintenance records. */
  maintenances: ProviderMaintenanceRecord[];
  /** NextMaintenanceID is the next provider maintenance identifier. */
  nextMaintenanceId: Long;
  /** Registrations is the list of provider registration records to import at genesis. */
  registrations: ProviderRegistration[];
}

function createBaseGenesisState(): GenesisState {
  return { providers: [], params: undefined, maintenances: [], nextMaintenanceId: Long.UZERO, registrations: [] };
}

export const GenesisState: MessageFns<GenesisState, "akash.provider.v1beta4.GenesisState"> = {
  $type: "akash.provider.v1beta4.GenesisState" as const,

  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.providers) {
      Provider.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.params !== undefined) {
      ProviderMaintenanceParams.encode(message.params, writer.uint32(18).fork()).join();
    }
    for (const v of message.maintenances) {
      ProviderMaintenanceRecord.encode(v!, writer.uint32(26).fork()).join();
    }
    if (!message.nextMaintenanceId.equals(Long.UZERO)) {
      writer.uint32(32).uint64(message.nextMaintenanceId.toString());
    }
    for (const v of message.registrations) {
      ProviderRegistration.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.providers.push(Provider.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.params = ProviderMaintenanceParams.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.maintenances.push(ProviderMaintenanceRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.nextMaintenanceId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.registrations.push(ProviderRegistration.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      providers: globalThis.Array.isArray(object?.providers)
        ? object.providers.map((e: any) => Provider.fromJSON(e))
        : [],
      params: isSet(object.params) ? ProviderMaintenanceParams.fromJSON(object.params) : undefined,
      maintenances: globalThis.Array.isArray(object?.maintenances)
        ? object.maintenances.map((e: any) => ProviderMaintenanceRecord.fromJSON(e))
        : [],
      nextMaintenanceId: isSet(object.next_maintenance_id) ? Long.fromValue(object.next_maintenance_id) : Long.UZERO,
      registrations: globalThis.Array.isArray(object?.registrations)
        ? object.registrations.map((e: any) => ProviderRegistration.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.providers?.length) {
      obj.providers = message.providers.map((e) => Provider.toJSON(e));
    }
    if (message.params !== undefined) {
      obj.params = ProviderMaintenanceParams.toJSON(message.params);
    }
    if (message.maintenances?.length) {
      obj.maintenances = message.maintenances.map((e) => ProviderMaintenanceRecord.toJSON(e));
    }
    if (!message.nextMaintenanceId.equals(Long.UZERO)) {
      obj.next_maintenance_id = (message.nextMaintenanceId || Long.UZERO).toString();
    }
    if (message.registrations?.length) {
      obj.registrations = message.registrations.map((e) => ProviderRegistration.toJSON(e));
    }
    return obj;
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.providers = object.providers?.map((e) => Provider.fromPartial(e)) || [];
    message.params = (object.params !== undefined && object.params !== null)
      ? ProviderMaintenanceParams.fromPartial(object.params)
      : undefined;
    message.maintenances = object.maintenances?.map((e) => ProviderMaintenanceRecord.fromPartial(e)) || [];
    message.nextMaintenanceId = (object.nextMaintenanceId !== undefined && object.nextMaintenanceId !== null)
      ? Long.fromValue(object.nextMaintenanceId)
      : Long.UZERO;
    message.registrations = object.registrations?.map((e) => ProviderRegistration.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
