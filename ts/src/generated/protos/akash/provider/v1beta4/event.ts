import { bytesFromBase64, base64FromBytes, toTimestamp, fromTimestamp, fromJsonTimestamp, numberToLong, isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: akash/provider/v1beta4/event.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Timestamp } from "../../../google/protobuf/timestamp.ts";
import {
  ProviderMaintenanceType,
  providerMaintenanceTypeFromJSON,
  providerMaintenanceTypeToJSON,
} from "./maintenance.ts";

/**
 * EventProviderCreated defines an SDK message for provider created event.
 * It contains all the required information to identify a provider on-chain.
 */
export interface EventProviderCreated {
  /**
   * Owner is the bech32 address of the account of the provider.
   * It is a string representing a valid account address.
   *
   * Example:
   *   "akash1..."
   */
  owner: string;
}

/**
 * EventProviderUpdated defines an SDK message for provider updated event.
 * It contains all the required information to identify a provider on-chain.
 */
export interface EventProviderUpdated {
  /**
   * Owner is the bech32 address of the account of the provider.
   * It is a string representing a valid account address.
   *
   * Example:
   *   "akash1..."
   */
  owner: string;
}

/** EventProviderDeleted defines an SDK message for provider deleted event. */
export interface EventProviderDeleted {
  /**
   * Owner is the bech32 address of the account of the provider.
   * It is a string representing a valid account address.
   *
   * Example:
   *   "akash1..."
   */
  owner: string;
}

/**
 * EventProviderMaintenanceOpened is emitted by the x/provider handler on a
 * successful MsgOpenProviderMaintenance. Tenants, indexers, REST services,
 * and Console use it to map the maintenance window to active leases owned by
 * the provider and to surface tenant-facing alerts off-chain.
 */
export interface EventProviderMaintenanceOpened {
  /** maintenance_id is the identifier assigned to the new maintenance record. */
  maintenanceId: Long;
  /**
   * provider is the bech32 address of the provider that opened the window.
   *
   * Example:
   *   "akash1..."
   */
  provider: string;
  /** maintenance_type is the declared category of the window. */
  maintenanceType: ProviderMaintenanceType;
  /** starts_at is the wall-clock time at which the maintenance window begins. */
  startsAt:
    | Date
    | undefined;
  /**
   * expected_ends_at is the wall-clock time at which the provider expects the
   * window to end.
   */
  expectedEndsAt:
    | Date
    | undefined;
  /**
   * metadata_hash is the optional, opaque commitment to off-chain explanatory
   * metadata supplied in MsgOpenProviderMaintenance.
   */
  metadataHash: Uint8Array;
}

/**
 * EventProviderMaintenanceClosed is emitted by the x/provider handler on a
 * successful MsgCloseProviderMaintenance. It signals to tenant-facing
 * clients that the provider has ended the window earlier than
 * expected_ends_at.
 */
export interface EventProviderMaintenanceClosed {
  /** maintenance_id is the identifier of the closed maintenance record. */
  maintenanceId: Long;
  /**
   * provider is the bech32 address of the provider that closed the window.
   *
   * Example:
   *   "akash1..."
   */
  provider: string;
  /** closed_at is the block time at which the handler closed the window. */
  closedAt: Date | undefined;
}

function createBaseEventProviderCreated(): EventProviderCreated {
  return { owner: "" };
}

export const EventProviderCreated: MessageFns<EventProviderCreated, "akash.provider.v1beta4.EventProviderCreated"> = {
  $type: "akash.provider.v1beta4.EventProviderCreated" as const,

  encode(message: EventProviderCreated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventProviderCreated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventProviderCreated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventProviderCreated {
    return { owner: isSet(object.owner) ? globalThis.String(object.owner) : "" };
  },

  toJSON(message: EventProviderCreated): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventProviderCreated>): EventProviderCreated {
    const message = createBaseEventProviderCreated();
    message.owner = object.owner ?? "";
    return message;
  },
};

function createBaseEventProviderUpdated(): EventProviderUpdated {
  return { owner: "" };
}

export const EventProviderUpdated: MessageFns<EventProviderUpdated, "akash.provider.v1beta4.EventProviderUpdated"> = {
  $type: "akash.provider.v1beta4.EventProviderUpdated" as const,

  encode(message: EventProviderUpdated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventProviderUpdated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventProviderUpdated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventProviderUpdated {
    return { owner: isSet(object.owner) ? globalThis.String(object.owner) : "" };
  },

  toJSON(message: EventProviderUpdated): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventProviderUpdated>): EventProviderUpdated {
    const message = createBaseEventProviderUpdated();
    message.owner = object.owner ?? "";
    return message;
  },
};

function createBaseEventProviderDeleted(): EventProviderDeleted {
  return { owner: "" };
}

export const EventProviderDeleted: MessageFns<EventProviderDeleted, "akash.provider.v1beta4.EventProviderDeleted"> = {
  $type: "akash.provider.v1beta4.EventProviderDeleted" as const,

  encode(message: EventProviderDeleted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventProviderDeleted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventProviderDeleted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventProviderDeleted {
    return { owner: isSet(object.owner) ? globalThis.String(object.owner) : "" };
  },

  toJSON(message: EventProviderDeleted): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventProviderDeleted>): EventProviderDeleted {
    const message = createBaseEventProviderDeleted();
    message.owner = object.owner ?? "";
    return message;
  },
};

function createBaseEventProviderMaintenanceOpened(): EventProviderMaintenanceOpened {
  return {
    maintenanceId: Long.UZERO,
    provider: "",
    maintenanceType: 0,
    startsAt: undefined,
    expectedEndsAt: undefined,
    metadataHash: new Uint8Array(0),
  };
}

export const EventProviderMaintenanceOpened: MessageFns<
  EventProviderMaintenanceOpened,
  "akash.provider.v1beta4.EventProviderMaintenanceOpened"
> = {
  $type: "akash.provider.v1beta4.EventProviderMaintenanceOpened" as const,

  encode(message: EventProviderMaintenanceOpened, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.maintenanceId.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.maintenanceId.toString());
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    if (message.maintenanceType !== 0) {
      writer.uint32(24).int32(message.maintenanceType);
    }
    if (message.startsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startsAt), writer.uint32(34).fork()).join();
    }
    if (message.expectedEndsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expectedEndsAt), writer.uint32(42).fork()).join();
    }
    if (message.metadataHash.length !== 0) {
      writer.uint32(50).bytes(message.metadataHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventProviderMaintenanceOpened {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventProviderMaintenanceOpened();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maintenanceId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maintenanceType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.startsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.expectedEndsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.metadataHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventProviderMaintenanceOpened {
    return {
      maintenanceId: isSet(object.maintenance_id) ? Long.fromValue(object.maintenance_id) : Long.UZERO,
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      maintenanceType: isSet(object.maintenance_type) ? providerMaintenanceTypeFromJSON(object.maintenance_type) : 0,
      startsAt: isSet(object.starts_at) ? fromJsonTimestamp(object.starts_at) : undefined,
      expectedEndsAt: isSet(object.expected_ends_at) ? fromJsonTimestamp(object.expected_ends_at) : undefined,
      metadataHash: isSet(object.metadata_hash) ? bytesFromBase64(object.metadata_hash) : new Uint8Array(0),
    };
  },

  toJSON(message: EventProviderMaintenanceOpened): unknown {
    const obj: any = {};
    if (!message.maintenanceId.equals(Long.UZERO)) {
      obj.maintenance_id = (message.maintenanceId || Long.UZERO).toString();
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.maintenanceType !== 0) {
      obj.maintenance_type = providerMaintenanceTypeToJSON(message.maintenanceType);
    }
    if (message.startsAt !== undefined) {
      obj.starts_at = message.startsAt.toISOString();
    }
    if (message.expectedEndsAt !== undefined) {
      obj.expected_ends_at = message.expectedEndsAt.toISOString();
    }
    if (message.metadataHash.length !== 0) {
      obj.metadata_hash = base64FromBytes(message.metadataHash);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventProviderMaintenanceOpened>): EventProviderMaintenanceOpened {
    const message = createBaseEventProviderMaintenanceOpened();
    message.maintenanceId = (object.maintenanceId !== undefined && object.maintenanceId !== null)
      ? Long.fromValue(object.maintenanceId)
      : Long.UZERO;
    message.provider = object.provider ?? "";
    message.maintenanceType = object.maintenanceType ?? 0;
    message.startsAt = object.startsAt ?? undefined;
    message.expectedEndsAt = object.expectedEndsAt ?? undefined;
    message.metadataHash = object.metadataHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEventProviderMaintenanceClosed(): EventProviderMaintenanceClosed {
  return { maintenanceId: Long.UZERO, provider: "", closedAt: undefined };
}

export const EventProviderMaintenanceClosed: MessageFns<
  EventProviderMaintenanceClosed,
  "akash.provider.v1beta4.EventProviderMaintenanceClosed"
> = {
  $type: "akash.provider.v1beta4.EventProviderMaintenanceClosed" as const,

  encode(message: EventProviderMaintenanceClosed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.maintenanceId.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.maintenanceId.toString());
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    if (message.closedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.closedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventProviderMaintenanceClosed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventProviderMaintenanceClosed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maintenanceId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.closedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventProviderMaintenanceClosed {
    return {
      maintenanceId: isSet(object.maintenance_id) ? Long.fromValue(object.maintenance_id) : Long.UZERO,
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      closedAt: isSet(object.closed_at) ? fromJsonTimestamp(object.closed_at) : undefined,
    };
  },

  toJSON(message: EventProviderMaintenanceClosed): unknown {
    const obj: any = {};
    if (!message.maintenanceId.equals(Long.UZERO)) {
      obj.maintenance_id = (message.maintenanceId || Long.UZERO).toString();
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.closedAt !== undefined) {
      obj.closed_at = message.closedAt.toISOString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventProviderMaintenanceClosed>): EventProviderMaintenanceClosed {
    const message = createBaseEventProviderMaintenanceClosed();
    message.maintenanceId = (object.maintenanceId !== undefined && object.maintenanceId !== null)
      ? Long.fromValue(object.maintenanceId)
      : Long.UZERO;
    message.provider = object.provider ?? "";
    message.closedAt = object.closedAt ?? undefined;
    return message;
  },
};

function _unused_bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function _unused_base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function _unused_fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function _unused_fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function _unused_numberToLong(number: number) {
  return Long.fromNumber(number);
}

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
