import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: akash/verification/v1/verificationrequirement.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import {
  AuditorSelectionMode,
  auditorSelectionModeFromJSON,
  auditorSelectionModeToJSON,
  CapabilityFlag,
  capabilityFlagFromJSON,
  capabilityFlagToJSON,
  VerificationTier,
  verificationTierFromJSON,
  verificationTierToJSON,
} from "./types.ts";

/**
 * VerificationRequirement holds the verification constraints that a deployment
 * places on the providers eligible to bid on a group. It is referenced from the
 * deployment-module `PlacementRequirements` message; this file is split out from
 * `state.proto` so the deployment-side import surface stays minimal (only the
 * shared enums in `types.proto` need to come along).
 * A `min_tier` of 0 (TierUnspecified) means "no requirement".
 */
export interface VerificationRequirement {
  /**
   * MinTier is the minimum verification tier required of bidding providers.
   * A value of 0 (TierUnspecified) means no tier requirement.
   */
  minTier: VerificationTier;
  /** RequiredCapabilities is the set of capability flags providers must assert. */
  requiredCapabilities: CapabilityFlag[];
  /**
   * RequiredAuditors is an optional list of specific auditor bech32 addresses
   * whose attestations must be present on the provider, interpreted per
   * `auditor_mode`.
   */
  requiredAuditors: string[];
  /**
   * AuditorMode controls how `required_auditors` is evaluated.
   * AuditorSelectionModeUnspecified is treated as Any.
   */
  auditorMode: AuditorSelectionMode;
  /**
   * MinAuditorCount is the minimum number of independent auditors that must
   * have attested the provider, regardless of identity.
   */
  minAuditorCount: number;
}

function createBaseVerificationRequirement(): VerificationRequirement {
  return { minTier: 0, requiredCapabilities: [], requiredAuditors: [], auditorMode: 0, minAuditorCount: 0 };
}

export const VerificationRequirement: MessageFns<
  VerificationRequirement,
  "akash.verification.v1.VerificationRequirement"
> = {
  $type: "akash.verification.v1.VerificationRequirement" as const,

  encode(message: VerificationRequirement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.minTier !== 0) {
      writer.uint32(8).int32(message.minTier);
    }
    for (const v of message.requiredCapabilities) {
      writer.uint32(16).int32(v!);
    }
    for (const v of message.requiredAuditors) {
      writer.uint32(26).string(v!);
    }
    if (message.auditorMode !== 0) {
      writer.uint32(32).int32(message.auditorMode);
    }
    if (message.minAuditorCount !== 0) {
      writer.uint32(40).uint32(message.minAuditorCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerificationRequirement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerificationRequirement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.minTier = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.requiredCapabilities.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.requiredCapabilities.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.requiredAuditors.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.auditorMode = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.minAuditorCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerificationRequirement {
    return {
      minTier: isSet(object.min_tier) ? verificationTierFromJSON(object.min_tier) : 0,
      requiredCapabilities: globalThis.Array.isArray(object?.required_capabilities)
        ? object.required_capabilities.map((e: any) => capabilityFlagFromJSON(e))
        : [],
      requiredAuditors: globalThis.Array.isArray(object?.required_auditors)
        ? object.required_auditors.map((e: any) => globalThis.String(e))
        : [],
      auditorMode: isSet(object.auditor_mode) ? auditorSelectionModeFromJSON(object.auditor_mode) : 0,
      minAuditorCount: isSet(object.min_auditor_count) ? globalThis.Number(object.min_auditor_count) : 0,
    };
  },

  toJSON(message: VerificationRequirement): unknown {
    const obj: any = {};
    if (message.minTier !== 0) {
      obj.min_tier = verificationTierToJSON(message.minTier);
    }
    if (message.requiredCapabilities?.length) {
      obj.required_capabilities = message.requiredCapabilities.map((e) => capabilityFlagToJSON(e));
    }
    if (message.requiredAuditors?.length) {
      obj.required_auditors = message.requiredAuditors;
    }
    if (message.auditorMode !== 0) {
      obj.auditor_mode = auditorSelectionModeToJSON(message.auditorMode);
    }
    if (message.minAuditorCount !== 0) {
      obj.min_auditor_count = Math.round(message.minAuditorCount);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<VerificationRequirement>): VerificationRequirement {
    const message = createBaseVerificationRequirement();
    message.minTier = object.minTier ?? 0;
    message.requiredCapabilities = object.requiredCapabilities?.map((e) => e) || [];
    message.requiredAuditors = object.requiredAuditors?.map((e) => e) || [];
    message.auditorMode = object.auditorMode ?? 0;
    message.minAuditorCount = object.minAuditorCount ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
