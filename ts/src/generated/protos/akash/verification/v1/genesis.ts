import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: akash/verification/v1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Params } from "./params.ts";
import {
  AttestationRecord,
  AuditEscrowRecord,
  AuditorRecord,
  DiscrepancyEvent,
  ProviderBondRecord,
  ProviderSnapshotRecord,
  ProviderVerificationGraceRecord,
} from "./state.ts";

/**
 * GenesisState defines the genesis state of the verification module. It
 * contains the on-chain parameters and the full set of records the module
 * is responsible for: auditors, attestations, discrepancies (and the next ID
 * counter), provider bonds, provider snapshot records, audit escrows (and
 * the next ID counter), and provider verification grace records (and the
 * next ID counter).
 */
export interface GenesisState {
  /** Params holds the on-chain parameters for the verification module. */
  params:
    | Params
    | undefined;
  /** Auditors is the list of registered auditor records. */
  auditors: AuditorRecord[];
  /** Attestations is the list of attestation records. */
  attestations: AttestationRecord[];
  /** Discrepancies is the list of discrepancy events. */
  discrepancies: DiscrepancyEvent[];
  /** ProviderBonds is the list of provider bond records. */
  providerBonds: ProviderBondRecord[];
  /** ProviderSnapshots is the list of provider snapshot records. */
  providerSnapshots: ProviderSnapshotRecord[];
  /**
   * NextDiscrepancyID is the monotonically increasing ID assigned to the next
   * discrepancy event.
   */
  nextDiscrepancyId: Long;
  /** AuditEscrows is the list of audit escrow records. */
  auditEscrows: AuditEscrowRecord[];
  /**
   * NextAuditEscrowID is the monotonically increasing ID assigned to the next
   * audit escrow record.
   */
  nextAuditEscrowId: Long;
  /** VerificationGraces is the list of provider verification grace records. */
  verificationGraces: ProviderVerificationGraceRecord[];
  /**
   * NextGraceRecordID is the monotonically increasing ID assigned to the next
   * provider verification grace record.
   */
  nextGraceRecordId: Long;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    auditors: [],
    attestations: [],
    discrepancies: [],
    providerBonds: [],
    providerSnapshots: [],
    nextDiscrepancyId: Long.UZERO,
    auditEscrows: [],
    nextAuditEscrowId: Long.UZERO,
    verificationGraces: [],
    nextGraceRecordId: Long.UZERO,
  };
}

export const GenesisState: MessageFns<GenesisState, "akash.verification.v1.GenesisState"> = {
  $type: "akash.verification.v1.GenesisState" as const,

  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    for (const v of message.auditors) {
      AuditorRecord.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.attestations) {
      AttestationRecord.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.discrepancies) {
      DiscrepancyEvent.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.providerBonds) {
      ProviderBondRecord.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.providerSnapshots) {
      ProviderSnapshotRecord.encode(v!, writer.uint32(50).fork()).join();
    }
    if (!message.nextDiscrepancyId.equals(Long.UZERO)) {
      writer.uint32(56).uint64(message.nextDiscrepancyId.toString());
    }
    for (const v of message.auditEscrows) {
      AuditEscrowRecord.encode(v!, writer.uint32(66).fork()).join();
    }
    if (!message.nextAuditEscrowId.equals(Long.UZERO)) {
      writer.uint32(72).uint64(message.nextAuditEscrowId.toString());
    }
    for (const v of message.verificationGraces) {
      ProviderVerificationGraceRecord.encode(v!, writer.uint32(82).fork()).join();
    }
    if (!message.nextGraceRecordId.equals(Long.UZERO)) {
      writer.uint32(88).uint64(message.nextGraceRecordId.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.auditors.push(AuditorRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.attestations.push(AttestationRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.discrepancies.push(DiscrepancyEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.providerBonds.push(ProviderBondRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.providerSnapshots.push(ProviderSnapshotRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.nextDiscrepancyId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.auditEscrows.push(AuditEscrowRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.nextAuditEscrowId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.verificationGraces.push(ProviderVerificationGraceRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.nextGraceRecordId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      auditors: globalThis.Array.isArray(object?.auditors)
        ? object.auditors.map((e: any) => AuditorRecord.fromJSON(e))
        : [],
      attestations: globalThis.Array.isArray(object?.attestations)
        ? object.attestations.map((e: any) => AttestationRecord.fromJSON(e))
        : [],
      discrepancies: globalThis.Array.isArray(object?.discrepancies)
        ? object.discrepancies.map((e: any) => DiscrepancyEvent.fromJSON(e))
        : [],
      providerBonds: globalThis.Array.isArray(object?.provider_bonds)
        ? object.provider_bonds.map((e: any) => ProviderBondRecord.fromJSON(e))
        : [],
      providerSnapshots: globalThis.Array.isArray(object?.provider_snapshots)
        ? object.provider_snapshots.map((e: any) => ProviderSnapshotRecord.fromJSON(e))
        : [],
      nextDiscrepancyId: isSet(object.next_discrepancy_id) ? Long.fromValue(object.next_discrepancy_id) : Long.UZERO,
      auditEscrows: globalThis.Array.isArray(object?.audit_escrows)
        ? object.audit_escrows.map((e: any) => AuditEscrowRecord.fromJSON(e))
        : [],
      nextAuditEscrowId: isSet(object.next_audit_escrow_id) ? Long.fromValue(object.next_audit_escrow_id) : Long.UZERO,
      verificationGraces: globalThis.Array.isArray(object?.verification_graces)
        ? object.verification_graces.map((e: any) => ProviderVerificationGraceRecord.fromJSON(e))
        : [],
      nextGraceRecordId: isSet(object.next_grace_record_id) ? Long.fromValue(object.next_grace_record_id) : Long.UZERO,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.auditors?.length) {
      obj.auditors = message.auditors.map((e) => AuditorRecord.toJSON(e));
    }
    if (message.attestations?.length) {
      obj.attestations = message.attestations.map((e) => AttestationRecord.toJSON(e));
    }
    if (message.discrepancies?.length) {
      obj.discrepancies = message.discrepancies.map((e) => DiscrepancyEvent.toJSON(e));
    }
    if (message.providerBonds?.length) {
      obj.provider_bonds = message.providerBonds.map((e) => ProviderBondRecord.toJSON(e));
    }
    if (message.providerSnapshots?.length) {
      obj.provider_snapshots = message.providerSnapshots.map((e) => ProviderSnapshotRecord.toJSON(e));
    }
    if (!message.nextDiscrepancyId.equals(Long.UZERO)) {
      obj.next_discrepancy_id = (message.nextDiscrepancyId || Long.UZERO).toString();
    }
    if (message.auditEscrows?.length) {
      obj.audit_escrows = message.auditEscrows.map((e) => AuditEscrowRecord.toJSON(e));
    }
    if (!message.nextAuditEscrowId.equals(Long.UZERO)) {
      obj.next_audit_escrow_id = (message.nextAuditEscrowId || Long.UZERO).toString();
    }
    if (message.verificationGraces?.length) {
      obj.verification_graces = message.verificationGraces.map((e) => ProviderVerificationGraceRecord.toJSON(e));
    }
    if (!message.nextGraceRecordId.equals(Long.UZERO)) {
      obj.next_grace_record_id = (message.nextGraceRecordId || Long.UZERO).toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.auditors = object.auditors?.map((e) => AuditorRecord.fromPartial(e)) || [];
    message.attestations = object.attestations?.map((e) => AttestationRecord.fromPartial(e)) || [];
    message.discrepancies = object.discrepancies?.map((e) => DiscrepancyEvent.fromPartial(e)) || [];
    message.providerBonds = object.providerBonds?.map((e) => ProviderBondRecord.fromPartial(e)) || [];
    message.providerSnapshots = object.providerSnapshots?.map((e) => ProviderSnapshotRecord.fromPartial(e)) || [];
    message.nextDiscrepancyId = (object.nextDiscrepancyId !== undefined && object.nextDiscrepancyId !== null)
      ? Long.fromValue(object.nextDiscrepancyId)
      : Long.UZERO;
    message.auditEscrows = object.auditEscrows?.map((e) => AuditEscrowRecord.fromPartial(e)) || [];
    message.nextAuditEscrowId = (object.nextAuditEscrowId !== undefined && object.nextAuditEscrowId !== null)
      ? Long.fromValue(object.nextAuditEscrowId)
      : Long.UZERO;
    message.verificationGraces =
      object.verificationGraces?.map((e) => ProviderVerificationGraceRecord.fromPartial(e)) || [];
    message.nextGraceRecordId = (object.nextGraceRecordId !== undefined && object.nextGraceRecordId !== null)
      ? Long.fromValue(object.nextGraceRecordId)
      : Long.UZERO;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
