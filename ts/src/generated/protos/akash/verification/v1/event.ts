import { bytesFromBase64, base64FromBytes, toTimestamp, fromTimestamp, fromJsonTimestamp, numberToLong, isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: akash/verification/v1/event.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";
import { Timestamp } from "../../../google/protobuf/timestamp.ts";
import {
  AttestationRevocationReason,
  attestationRevocationReasonFromJSON,
  attestationRevocationReasonToJSON,
  AuditEscrowSettlementReason,
  auditEscrowSettlementReasonFromJSON,
  auditEscrowSettlementReasonToJSON,
  CapabilityFlag,
  capabilityFlagFromJSON,
  capabilityFlagToJSON,
  DiscrepancyResolutionReason,
  discrepancyResolutionReasonFromJSON,
  discrepancyResolutionReasonToJSON,
  FaultAttribution,
  faultAttributionFromJSON,
  faultAttributionToJSON,
  ProviderBondSlashReason,
  providerBondSlashReasonFromJSON,
  providerBondSlashReasonToJSON,
  VerificationGraceStatus,
  verificationGraceStatusFromJSON,
  verificationGraceStatusToJSON,
  VerificationTier,
  verificationTierFromJSON,
  verificationTierToJSON,
  VoidedReason,
  voidedReasonFromJSON,
  voidedReasonToJSON,
} from "./types.ts";

/** EventAuditorRegistered is emitted when a new auditor is registered. */
export interface EventAuditorRegistered {
  /** Auditor is the bech32 account address of the newly registered auditor. */
  auditor: string;
  /** MaxAttestationTier is the maximum tier the auditor may attest at. */
  maxAttestationTier: VerificationTier;
}

/** EventAuditorBondPosted is emitted when an auditor posts (or tops up) bond. */
export interface EventAuditorBondPosted {
  /** Auditor is the bech32 account address of the bonding auditor. */
  auditor: string;
  /** Amount is the coin amount posted as bond. */
  amount: Coin | undefined;
}

/**
 * EventAuditorFrozen is emitted when an auditor crosses the discrepancy
 * threshold and is frozen pending governance review.
 */
export interface EventAuditorFrozen {
  /** Auditor is the bech32 account address of the frozen auditor. */
  auditor: string;
  /** DiscrepancyID is the identifier of the discrepancy that triggered the freeze. */
  discrepancyId: Long;
}

/**
 * EventAuditorLapsed is emitted when an auditor's renewal deadline passes
 * without renewal.
 */
export interface EventAuditorLapsed {
  /** Auditor is the bech32 account address of the lapsed auditor. */
  auditor: string;
}

/** EventAuditorResigned is emitted when an auditor voluntarily resigns. */
export interface EventAuditorResigned {
  /** Auditor is the bech32 account address of the resigning auditor. */
  auditor: string;
}

/** EventAuditorRemoved is emitted when governance removes an auditor. */
export interface EventAuditorRemoved {
  /** Auditor is the bech32 account address of the removed auditor. */
  auditor: string;
}

/** EventAuditorRenewed is emitted when an auditor's registration is renewed. */
export interface EventAuditorRenewed {
  /** Auditor is the bech32 account address of the renewed auditor. */
  auditor: string;
  /** NewDeadline is the new renewal deadline assigned to the auditor. */
  newDeadline: Date | undefined;
}

/** EventAttestationSubmitted is emitted when an auditor submits an attestation. */
export interface EventAttestationSubmitted {
  /** Provider is the bech32 account address of the attested provider. */
  provider: string;
  /** Auditor is the bech32 account address of the issuing auditor. */
  auditor: string;
  /** Tier is the verification tier asserted by the attestation. */
  tier: VerificationTier;
  /** Capabilities is the set of capability flags asserted by the attestation. */
  capabilities: CapabilityFlag[];
  /** ExpiresAt is the chain timestamp at which the attestation expires. */
  expiresAt:
    | Date
    | undefined;
  /** AuditEscrowID is the audit escrow identifier that authorized the attestation. */
  auditEscrowId: Long;
}

/** EventAttestationExpired is emitted when an attestation expires. */
export interface EventAttestationExpired {
  /** Provider is the bech32 account address of the provider whose attestation expired. */
  provider: string;
  /** Auditor is the bech32 account address of the auditor whose attestation expired. */
  auditor: string;
  /** Tier is the tier of the expired attestation. */
  tier: VerificationTier;
}

/**
 * EventAttestationReplaced is emitted when a new attestation replaces an existing
 * attestation from the same auditor for the same provider.
 */
export interface EventAttestationReplaced {
  /** Provider is the bech32 account address of the provider involved. */
  provider: string;
  /** Auditor is the bech32 account address of the auditor involved. */
  auditor: string;
  /** OldTier is the tier of the previous attestation being replaced. */
  oldTier: VerificationTier;
  /** NewTier is the tier of the new attestation. */
  newTier: VerificationTier;
  /** OldAuditEscrowID is the audit escrow that authorized the previous attestation. */
  oldAuditEscrowId: Long;
  /** NewAuditEscrowID is the audit escrow that authorized the new attestation. */
  newAuditEscrowId: Long;
}

/** EventAttestationRevoked is emitted when an attestation is revoked by the auditor. */
export interface EventAttestationRevoked {
  /** Provider is the bech32 account address of the provider involved. */
  provider: string;
  /** Auditor is the bech32 account address of the auditor involved. */
  auditor: string;
  /**
   * Initiator is the textual identifier of the party that initiated the
   * revocation (e.g. "auditor", "governance").
   */
  initiator: string;
  /** Reason is the typed reason recorded for the revocation. */
  reason: AttestationRevocationReason;
}

/** EventAttestationVoided is emitted when an attestation transitions to voided state. */
export interface EventAttestationVoided {
  /** Provider is the bech32 account address of the provider whose attestation was voided. */
  provider: string;
  /** Auditor is the bech32 account address of the auditor whose attestation was voided. */
  auditor: string;
  /** Reason is the typed reason recorded for the void. */
  reason: VoidedReason;
}

/**
 * EventDiscrepancyDetected is emitted when the chain detects two conflicting
 * attestations from different auditors for the same provider.
 */
export interface EventDiscrepancyDetected {
  /** DiscrepancyID is the identifier of the newly created discrepancy record. */
  discrepancyId: Long;
  /** Provider is the bech32 account address of the disputed provider. */
  provider: string;
  /** AuditorA is the bech32 account address of the first auditor in the dispute. */
  auditorA: string;
  /** TierA is the tier asserted by auditor A. */
  tierA: VerificationTier;
  /** AuditorB is the bech32 account address of the second auditor in the dispute. */
  auditorB: string;
  /** TierB is the tier asserted by auditor B. */
  tierB: VerificationTier;
}

/** EventDiscrepancyResolved is emitted when a discrepancy is resolved by governance. */
export interface EventDiscrepancyResolved {
  /** DiscrepancyID is the identifier of the resolved discrepancy record. */
  discrepancyId: Long;
  /**
   * VindicatedAuditor is the identifier of the auditor vindicated by the
   * resolution; carries an empty string when neither party is vindicated.
   */
  vindicatedAuditor: string;
  /** Reason is the typed resolution reason recorded by governance. */
  reason: DiscrepancyResolutionReason;
  /** FaultAttribution is the typed fault attribution recorded at resolution. */
  faultAttribution: FaultAttribution;
}

/**
 * EventDiscrepancyTimedOut is emitted when a discrepancy's resolution window
 * elapses without governance action.
 */
export interface EventDiscrepancyTimedOut {
  /** DiscrepancyID is the identifier of the timed-out discrepancy record. */
  discrepancyId: Long;
  /** AuditorA is the bech32 account address of the first auditor in the dispute. */
  auditorA: string;
  /** AuditorB is the bech32 account address of the second auditor in the dispute. */
  auditorB: string;
}

/** EventProviderBondPosted is emitted when a provider posts (or tops up) bond. */
export interface EventProviderBondPosted {
  /** Provider is the bech32 account address of the bonding provider. */
  provider: string;
  /** Amount is the coin amount just posted in this transaction. */
  amount:
    | Coin
    | undefined;
  /** TotalBonded is the resulting total bonded amount after this posting. */
  totalBonded: Coin | undefined;
}

/** EventProviderBondSlashed is emitted when a provider's bond is slashed. */
export interface EventProviderBondSlashed {
  /** Provider is the bech32 account address of the slashed provider. */
  provider: string;
  /** SlashedAmount is the coin amount slashed in this action. */
  slashedAmount:
    | Coin
    | undefined;
  /** Reason is the typed slash reason recorded. */
  reason: ProviderBondSlashReason;
}

/**
 * EventProviderBondWithdrawalInitiated is emitted when a provider begins
 * unbonding part or all of its bond.
 */
export interface EventProviderBondWithdrawalInitiated {
  /** Provider is the bech32 account address of the unbonding provider. */
  provider: string;
  /** Amount is the coin amount the provider is unbonding. */
  amount:
    | Coin
    | undefined;
  /** CompletionTime is the chain timestamp at which the unbonding completes. */
  completionTime: Date | undefined;
}

/**
 * EventProviderBondWithdrawalCompleted is emitted when an unbonding entry
 * matures and the amount is returned to the provider.
 */
export interface EventProviderBondWithdrawalCompleted {
  /** Provider is the bech32 account address of the provider receiving the unbonded coins. */
  provider: string;
  /** Amount is the coin amount returned to the provider. */
  amount: Coin | undefined;
}

/** EventSnapshotHashPosted is emitted when a provider posts a snapshot hash. */
export interface EventSnapshotHashPosted {
  /** Provider is the bech32 account address of the snapshotting provider. */
  provider: string;
  /** SnapshotHash is the opaque hash bytes posted by the provider. */
  snapshotHash: Uint8Array;
  /** ComplianceDeadline is the chain timestamp by which the next snapshot must be posted. */
  complianceDeadline: Date | undefined;
}

/**
 * EventSnapshotSuspended is emitted when a provider's snapshot is suspended
 * for missing a compliance deadline.
 */
export interface EventSnapshotSuspended {
  /** Provider is the bech32 account address of the suspended provider. */
  provider: string;
}

/**
 * EventSnapshotResumed is emitted when a previously suspended provider snapshot
 * returns to compliance.
 */
export interface EventSnapshotResumed {
  /** Provider is the bech32 account address of the resumed provider. */
  provider: string;
}

/** EventFeeEscrowed is emitted when an audit fee is escrowed by a provider. */
export interface EventFeeEscrowed {
  /** Provider is the bech32 account address of the escrowing provider. */
  provider: string;
  /** Auditor is the bech32 account address of the consuming auditor (may be empty). */
  auditor: string;
  /** Amount is the coin amount escrowed. */
  amount: Coin | undefined;
}

/** EventFeeReleasedToAuditor is emitted when an escrowed fee is released to the auditor. */
export interface EventFeeReleasedToAuditor {
  /** Auditor is the bech32 account address of the auditor receiving the fee. */
  auditor: string;
  /** Amount is the coin amount released to the auditor. */
  amount: Coin | undefined;
}

/** EventFeeReturnedToProvider is emitted when an escrowed fee is returned to the provider. */
export interface EventFeeReturnedToProvider {
  /** Provider is the bech32 account address of the provider receiving the fee. */
  provider: string;
  /** Amount is the coin amount returned to the provider. */
  amount: Coin | undefined;
}

/** EventAuditEscrowOpened is emitted when a provider opens an audit escrow. */
export interface EventAuditEscrowOpened {
  /** AuditEscrowID is the identifier of the newly opened audit escrow. */
  auditEscrowId: Long;
  /** Provider is the bech32 account address of the provider opening the escrow. */
  provider: string;
  /** Fee is the audit fee escrowed. */
  fee:
    | Coin
    | undefined;
  /** ProviderDeposit is the deposit a provider escrows alongside the fee. */
  providerDeposit: Coin | undefined;
}

/** EventAuditEscrowSettled is emitted when an audit escrow reaches final settlement. */
export interface EventAuditEscrowSettled {
  /** AuditEscrowID is the identifier of the settled audit escrow. */
  auditEscrowId: Long;
  /** Reason is the typed settlement reason recorded. */
  reason: AuditEscrowSettlementReason;
  /** FaultAttribution is the typed fault attribution recorded at settlement. */
  faultAttribution: FaultAttribution;
}

/**
 * EventDepositReturnedToAuditor is emitted when an auditor's anti-griefing
 * deposit is returned.
 */
export interface EventDepositReturnedToAuditor {
  /** Auditor is the bech32 account address of the auditor receiving the deposit. */
  auditor: string;
  /** Amount is the coin amount returned to the auditor. */
  amount: Coin | undefined;
}

/** EventDepositSlashed is emitted when an auditor's anti-griefing deposit is slashed. */
export interface EventDepositSlashed {
  /** Auditor is the bech32 account address of the auditor whose deposit was slashed. */
  auditor: string;
  /** Amount is the coin amount slashed from the deposit. */
  amount: Coin | undefined;
}

/**
 * EventVerificationGraceStarted is emitted when a provider enters a verification
 * grace window.
 */
export interface EventVerificationGraceStarted {
  /** GraceRecordID is the identifier of the newly opened grace record. */
  graceRecordId: Long;
  /** Provider is the bech32 account address of the provider entering grace. */
  provider: string;
  /** PreservedTier is the verification tier preserved for the provider during grace. */
  preservedTier: VerificationTier;
}

/**
 * EventVerificationGraceEnded is emitted when a verification grace window
 * concludes for any reason.
 */
export interface EventVerificationGraceEnded {
  /** GraceRecordID is the identifier of the closed grace record. */
  graceRecordId: Long;
  /** Status is the final lifecycle status of the closed grace record. */
  status: VerificationGraceStatus;
}

function createBaseEventAuditorRegistered(): EventAuditorRegistered {
  return { auditor: "", maxAttestationTier: 0 };
}

export const EventAuditorRegistered: MessageFns<
  EventAuditorRegistered,
  "akash.verification.v1.EventAuditorRegistered"
> = {
  $type: "akash.verification.v1.EventAuditorRegistered" as const,

  encode(message: EventAuditorRegistered, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditor !== "") {
      writer.uint32(10).string(message.auditor);
    }
    if (message.maxAttestationTier !== 0) {
      writer.uint32(16).int32(message.maxAttestationTier);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAuditorRegistered {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAuditorRegistered();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxAttestationTier = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAuditorRegistered {
    return {
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      maxAttestationTier: isSet(object.max_attestation_tier)
        ? verificationTierFromJSON(object.max_attestation_tier)
        : 0,
    };
  },

  toJSON(message: EventAuditorRegistered): unknown {
    const obj: any = {};
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.maxAttestationTier !== 0) {
      obj.max_attestation_tier = verificationTierToJSON(message.maxAttestationTier);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAuditorRegistered>): EventAuditorRegistered {
    const message = createBaseEventAuditorRegistered();
    message.auditor = object.auditor ?? "";
    message.maxAttestationTier = object.maxAttestationTier ?? 0;
    return message;
  },
};

function createBaseEventAuditorBondPosted(): EventAuditorBondPosted {
  return { auditor: "", amount: undefined };
}

export const EventAuditorBondPosted: MessageFns<
  EventAuditorBondPosted,
  "akash.verification.v1.EventAuditorBondPosted"
> = {
  $type: "akash.verification.v1.EventAuditorBondPosted" as const,

  encode(message: EventAuditorBondPosted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditor !== "") {
      writer.uint32(10).string(message.auditor);
    }
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAuditorBondPosted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAuditorBondPosted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAuditorBondPosted {
    return {
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
    };
  },

  toJSON(message: EventAuditorBondPosted): unknown {
    const obj: any = {};
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAuditorBondPosted>): EventAuditorBondPosted {
    const message = createBaseEventAuditorBondPosted();
    message.auditor = object.auditor ?? "";
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    return message;
  },
};

function createBaseEventAuditorFrozen(): EventAuditorFrozen {
  return { auditor: "", discrepancyId: Long.UZERO };
}

export const EventAuditorFrozen: MessageFns<EventAuditorFrozen, "akash.verification.v1.EventAuditorFrozen"> = {
  $type: "akash.verification.v1.EventAuditorFrozen" as const,

  encode(message: EventAuditorFrozen, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditor !== "") {
      writer.uint32(10).string(message.auditor);
    }
    if (!message.discrepancyId.equals(Long.UZERO)) {
      writer.uint32(16).uint64(message.discrepancyId.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAuditorFrozen {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAuditorFrozen();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.discrepancyId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAuditorFrozen {
    return {
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      discrepancyId: isSet(object.discrepancy_id) ? Long.fromValue(object.discrepancy_id) : Long.UZERO,
    };
  },

  toJSON(message: EventAuditorFrozen): unknown {
    const obj: any = {};
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (!message.discrepancyId.equals(Long.UZERO)) {
      obj.discrepancy_id = (message.discrepancyId || Long.UZERO).toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAuditorFrozen>): EventAuditorFrozen {
    const message = createBaseEventAuditorFrozen();
    message.auditor = object.auditor ?? "";
    message.discrepancyId = (object.discrepancyId !== undefined && object.discrepancyId !== null)
      ? Long.fromValue(object.discrepancyId)
      : Long.UZERO;
    return message;
  },
};

function createBaseEventAuditorLapsed(): EventAuditorLapsed {
  return { auditor: "" };
}

export const EventAuditorLapsed: MessageFns<EventAuditorLapsed, "akash.verification.v1.EventAuditorLapsed"> = {
  $type: "akash.verification.v1.EventAuditorLapsed" as const,

  encode(message: EventAuditorLapsed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditor !== "") {
      writer.uint32(10).string(message.auditor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAuditorLapsed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAuditorLapsed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAuditorLapsed {
    return { auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "" };
  },

  toJSON(message: EventAuditorLapsed): unknown {
    const obj: any = {};
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAuditorLapsed>): EventAuditorLapsed {
    const message = createBaseEventAuditorLapsed();
    message.auditor = object.auditor ?? "";
    return message;
  },
};

function createBaseEventAuditorResigned(): EventAuditorResigned {
  return { auditor: "" };
}

export const EventAuditorResigned: MessageFns<EventAuditorResigned, "akash.verification.v1.EventAuditorResigned"> = {
  $type: "akash.verification.v1.EventAuditorResigned" as const,

  encode(message: EventAuditorResigned, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditor !== "") {
      writer.uint32(10).string(message.auditor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAuditorResigned {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAuditorResigned();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAuditorResigned {
    return { auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "" };
  },

  toJSON(message: EventAuditorResigned): unknown {
    const obj: any = {};
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAuditorResigned>): EventAuditorResigned {
    const message = createBaseEventAuditorResigned();
    message.auditor = object.auditor ?? "";
    return message;
  },
};

function createBaseEventAuditorRemoved(): EventAuditorRemoved {
  return { auditor: "" };
}

export const EventAuditorRemoved: MessageFns<EventAuditorRemoved, "akash.verification.v1.EventAuditorRemoved"> = {
  $type: "akash.verification.v1.EventAuditorRemoved" as const,

  encode(message: EventAuditorRemoved, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditor !== "") {
      writer.uint32(10).string(message.auditor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAuditorRemoved {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAuditorRemoved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAuditorRemoved {
    return { auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "" };
  },

  toJSON(message: EventAuditorRemoved): unknown {
    const obj: any = {};
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAuditorRemoved>): EventAuditorRemoved {
    const message = createBaseEventAuditorRemoved();
    message.auditor = object.auditor ?? "";
    return message;
  },
};

function createBaseEventAuditorRenewed(): EventAuditorRenewed {
  return { auditor: "", newDeadline: undefined };
}

export const EventAuditorRenewed: MessageFns<EventAuditorRenewed, "akash.verification.v1.EventAuditorRenewed"> = {
  $type: "akash.verification.v1.EventAuditorRenewed" as const,

  encode(message: EventAuditorRenewed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditor !== "") {
      writer.uint32(10).string(message.auditor);
    }
    if (message.newDeadline !== undefined) {
      Timestamp.encode(toTimestamp(message.newDeadline), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAuditorRenewed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAuditorRenewed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newDeadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAuditorRenewed {
    return {
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      newDeadline: isSet(object.new_deadline) ? fromJsonTimestamp(object.new_deadline) : undefined,
    };
  },

  toJSON(message: EventAuditorRenewed): unknown {
    const obj: any = {};
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.newDeadline !== undefined) {
      obj.new_deadline = message.newDeadline.toISOString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAuditorRenewed>): EventAuditorRenewed {
    const message = createBaseEventAuditorRenewed();
    message.auditor = object.auditor ?? "";
    message.newDeadline = object.newDeadline ?? undefined;
    return message;
  },
};

function createBaseEventAttestationSubmitted(): EventAttestationSubmitted {
  return { provider: "", auditor: "", tier: 0, capabilities: [], expiresAt: undefined, auditEscrowId: Long.UZERO };
}

export const EventAttestationSubmitted: MessageFns<
  EventAttestationSubmitted,
  "akash.verification.v1.EventAttestationSubmitted"
> = {
  $type: "akash.verification.v1.EventAttestationSubmitted" as const,

  encode(message: EventAttestationSubmitted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.auditor !== "") {
      writer.uint32(18).string(message.auditor);
    }
    if (message.tier !== 0) {
      writer.uint32(24).int32(message.tier);
    }
    for (const v of message.capabilities) {
      writer.uint32(32).int32(v!);
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(42).fork()).join();
    }
    if (!message.auditEscrowId.equals(Long.UZERO)) {
      writer.uint32(48).uint64(message.auditEscrowId.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAttestationSubmitted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAttestationSubmitted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tier = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.capabilities.push(reader.int32() as any);

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.capabilities.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.auditEscrowId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAttestationSubmitted {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      tier: isSet(object.tier) ? verificationTierFromJSON(object.tier) : 0,
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => capabilityFlagFromJSON(e))
        : [],
      expiresAt: isSet(object.expires_at) ? fromJsonTimestamp(object.expires_at) : undefined,
      auditEscrowId: isSet(object.audit_escrow_id) ? Long.fromValue(object.audit_escrow_id) : Long.UZERO,
    };
  },

  toJSON(message: EventAttestationSubmitted): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.tier !== 0) {
      obj.tier = verificationTierToJSON(message.tier);
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => capabilityFlagToJSON(e));
    }
    if (message.expiresAt !== undefined) {
      obj.expires_at = message.expiresAt.toISOString();
    }
    if (!message.auditEscrowId.equals(Long.UZERO)) {
      obj.audit_escrow_id = (message.auditEscrowId || Long.UZERO).toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAttestationSubmitted>): EventAttestationSubmitted {
    const message = createBaseEventAttestationSubmitted();
    message.provider = object.provider ?? "";
    message.auditor = object.auditor ?? "";
    message.tier = object.tier ?? 0;
    message.capabilities = object.capabilities?.map((e) => e) || [];
    message.expiresAt = object.expiresAt ?? undefined;
    message.auditEscrowId = (object.auditEscrowId !== undefined && object.auditEscrowId !== null)
      ? Long.fromValue(object.auditEscrowId)
      : Long.UZERO;
    return message;
  },
};

function createBaseEventAttestationExpired(): EventAttestationExpired {
  return { provider: "", auditor: "", tier: 0 };
}

export const EventAttestationExpired: MessageFns<
  EventAttestationExpired,
  "akash.verification.v1.EventAttestationExpired"
> = {
  $type: "akash.verification.v1.EventAttestationExpired" as const,

  encode(message: EventAttestationExpired, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.auditor !== "") {
      writer.uint32(18).string(message.auditor);
    }
    if (message.tier !== 0) {
      writer.uint32(24).int32(message.tier);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAttestationExpired {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAttestationExpired();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tier = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAttestationExpired {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      tier: isSet(object.tier) ? verificationTierFromJSON(object.tier) : 0,
    };
  },

  toJSON(message: EventAttestationExpired): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.tier !== 0) {
      obj.tier = verificationTierToJSON(message.tier);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAttestationExpired>): EventAttestationExpired {
    const message = createBaseEventAttestationExpired();
    message.provider = object.provider ?? "";
    message.auditor = object.auditor ?? "";
    message.tier = object.tier ?? 0;
    return message;
  },
};

function createBaseEventAttestationReplaced(): EventAttestationReplaced {
  return {
    provider: "",
    auditor: "",
    oldTier: 0,
    newTier: 0,
    oldAuditEscrowId: Long.UZERO,
    newAuditEscrowId: Long.UZERO,
  };
}

export const EventAttestationReplaced: MessageFns<
  EventAttestationReplaced,
  "akash.verification.v1.EventAttestationReplaced"
> = {
  $type: "akash.verification.v1.EventAttestationReplaced" as const,

  encode(message: EventAttestationReplaced, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.auditor !== "") {
      writer.uint32(18).string(message.auditor);
    }
    if (message.oldTier !== 0) {
      writer.uint32(24).int32(message.oldTier);
    }
    if (message.newTier !== 0) {
      writer.uint32(32).int32(message.newTier);
    }
    if (!message.oldAuditEscrowId.equals(Long.UZERO)) {
      writer.uint32(40).uint64(message.oldAuditEscrowId.toString());
    }
    if (!message.newAuditEscrowId.equals(Long.UZERO)) {
      writer.uint32(48).uint64(message.newAuditEscrowId.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAttestationReplaced {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAttestationReplaced();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.oldTier = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.newTier = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.oldAuditEscrowId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.newAuditEscrowId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAttestationReplaced {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      oldTier: isSet(object.old_tier) ? verificationTierFromJSON(object.old_tier) : 0,
      newTier: isSet(object.new_tier) ? verificationTierFromJSON(object.new_tier) : 0,
      oldAuditEscrowId: isSet(object.old_audit_escrow_id) ? Long.fromValue(object.old_audit_escrow_id) : Long.UZERO,
      newAuditEscrowId: isSet(object.new_audit_escrow_id) ? Long.fromValue(object.new_audit_escrow_id) : Long.UZERO,
    };
  },

  toJSON(message: EventAttestationReplaced): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.oldTier !== 0) {
      obj.old_tier = verificationTierToJSON(message.oldTier);
    }
    if (message.newTier !== 0) {
      obj.new_tier = verificationTierToJSON(message.newTier);
    }
    if (!message.oldAuditEscrowId.equals(Long.UZERO)) {
      obj.old_audit_escrow_id = (message.oldAuditEscrowId || Long.UZERO).toString();
    }
    if (!message.newAuditEscrowId.equals(Long.UZERO)) {
      obj.new_audit_escrow_id = (message.newAuditEscrowId || Long.UZERO).toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAttestationReplaced>): EventAttestationReplaced {
    const message = createBaseEventAttestationReplaced();
    message.provider = object.provider ?? "";
    message.auditor = object.auditor ?? "";
    message.oldTier = object.oldTier ?? 0;
    message.newTier = object.newTier ?? 0;
    message.oldAuditEscrowId = (object.oldAuditEscrowId !== undefined && object.oldAuditEscrowId !== null)
      ? Long.fromValue(object.oldAuditEscrowId)
      : Long.UZERO;
    message.newAuditEscrowId = (object.newAuditEscrowId !== undefined && object.newAuditEscrowId !== null)
      ? Long.fromValue(object.newAuditEscrowId)
      : Long.UZERO;
    return message;
  },
};

function createBaseEventAttestationRevoked(): EventAttestationRevoked {
  return { provider: "", auditor: "", initiator: "", reason: 0 };
}

export const EventAttestationRevoked: MessageFns<
  EventAttestationRevoked,
  "akash.verification.v1.EventAttestationRevoked"
> = {
  $type: "akash.verification.v1.EventAttestationRevoked" as const,

  encode(message: EventAttestationRevoked, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.auditor !== "") {
      writer.uint32(18).string(message.auditor);
    }
    if (message.initiator !== "") {
      writer.uint32(26).string(message.initiator);
    }
    if (message.reason !== 0) {
      writer.uint32(32).int32(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAttestationRevoked {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAttestationRevoked();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.initiator = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAttestationRevoked {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      initiator: isSet(object.initiator) ? globalThis.String(object.initiator) : "",
      reason: isSet(object.reason) ? attestationRevocationReasonFromJSON(object.reason) : 0,
    };
  },

  toJSON(message: EventAttestationRevoked): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.initiator !== "") {
      obj.initiator = message.initiator;
    }
    if (message.reason !== 0) {
      obj.reason = attestationRevocationReasonToJSON(message.reason);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAttestationRevoked>): EventAttestationRevoked {
    const message = createBaseEventAttestationRevoked();
    message.provider = object.provider ?? "";
    message.auditor = object.auditor ?? "";
    message.initiator = object.initiator ?? "";
    message.reason = object.reason ?? 0;
    return message;
  },
};

function createBaseEventAttestationVoided(): EventAttestationVoided {
  return { provider: "", auditor: "", reason: 0 };
}

export const EventAttestationVoided: MessageFns<
  EventAttestationVoided,
  "akash.verification.v1.EventAttestationVoided"
> = {
  $type: "akash.verification.v1.EventAttestationVoided" as const,

  encode(message: EventAttestationVoided, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.auditor !== "") {
      writer.uint32(18).string(message.auditor);
    }
    if (message.reason !== 0) {
      writer.uint32(24).int32(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAttestationVoided {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAttestationVoided();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAttestationVoided {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      reason: isSet(object.reason) ? voidedReasonFromJSON(object.reason) : 0,
    };
  },

  toJSON(message: EventAttestationVoided): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.reason !== 0) {
      obj.reason = voidedReasonToJSON(message.reason);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAttestationVoided>): EventAttestationVoided {
    const message = createBaseEventAttestationVoided();
    message.provider = object.provider ?? "";
    message.auditor = object.auditor ?? "";
    message.reason = object.reason ?? 0;
    return message;
  },
};

function createBaseEventDiscrepancyDetected(): EventDiscrepancyDetected {
  return { discrepancyId: Long.UZERO, provider: "", auditorA: "", tierA: 0, auditorB: "", tierB: 0 };
}

export const EventDiscrepancyDetected: MessageFns<
  EventDiscrepancyDetected,
  "akash.verification.v1.EventDiscrepancyDetected"
> = {
  $type: "akash.verification.v1.EventDiscrepancyDetected" as const,

  encode(message: EventDiscrepancyDetected, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.discrepancyId.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.discrepancyId.toString());
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    if (message.auditorA !== "") {
      writer.uint32(26).string(message.auditorA);
    }
    if (message.tierA !== 0) {
      writer.uint32(32).int32(message.tierA);
    }
    if (message.auditorB !== "") {
      writer.uint32(42).string(message.auditorB);
    }
    if (message.tierB !== 0) {
      writer.uint32(48).int32(message.tierB);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventDiscrepancyDetected {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventDiscrepancyDetected();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.discrepancyId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.auditorA = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.tierA = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.auditorB = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.tierB = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventDiscrepancyDetected {
    return {
      discrepancyId: isSet(object.discrepancy_id) ? Long.fromValue(object.discrepancy_id) : Long.UZERO,
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      auditorA: isSet(object.auditor_a) ? globalThis.String(object.auditor_a) : "",
      tierA: isSet(object.tier_a) ? verificationTierFromJSON(object.tier_a) : 0,
      auditorB: isSet(object.auditor_b) ? globalThis.String(object.auditor_b) : "",
      tierB: isSet(object.tier_b) ? verificationTierFromJSON(object.tier_b) : 0,
    };
  },

  toJSON(message: EventDiscrepancyDetected): unknown {
    const obj: any = {};
    if (!message.discrepancyId.equals(Long.UZERO)) {
      obj.discrepancy_id = (message.discrepancyId || Long.UZERO).toString();
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.auditorA !== "") {
      obj.auditor_a = message.auditorA;
    }
    if (message.tierA !== 0) {
      obj.tier_a = verificationTierToJSON(message.tierA);
    }
    if (message.auditorB !== "") {
      obj.auditor_b = message.auditorB;
    }
    if (message.tierB !== 0) {
      obj.tier_b = verificationTierToJSON(message.tierB);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventDiscrepancyDetected>): EventDiscrepancyDetected {
    const message = createBaseEventDiscrepancyDetected();
    message.discrepancyId = (object.discrepancyId !== undefined && object.discrepancyId !== null)
      ? Long.fromValue(object.discrepancyId)
      : Long.UZERO;
    message.provider = object.provider ?? "";
    message.auditorA = object.auditorA ?? "";
    message.tierA = object.tierA ?? 0;
    message.auditorB = object.auditorB ?? "";
    message.tierB = object.tierB ?? 0;
    return message;
  },
};

function createBaseEventDiscrepancyResolved(): EventDiscrepancyResolved {
  return { discrepancyId: Long.UZERO, vindicatedAuditor: "", reason: 0, faultAttribution: 0 };
}

export const EventDiscrepancyResolved: MessageFns<
  EventDiscrepancyResolved,
  "akash.verification.v1.EventDiscrepancyResolved"
> = {
  $type: "akash.verification.v1.EventDiscrepancyResolved" as const,

  encode(message: EventDiscrepancyResolved, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.discrepancyId.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.discrepancyId.toString());
    }
    if (message.vindicatedAuditor !== "") {
      writer.uint32(18).string(message.vindicatedAuditor);
    }
    if (message.reason !== 0) {
      writer.uint32(24).int32(message.reason);
    }
    if (message.faultAttribution !== 0) {
      writer.uint32(32).int32(message.faultAttribution);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventDiscrepancyResolved {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventDiscrepancyResolved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.discrepancyId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.vindicatedAuditor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.faultAttribution = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventDiscrepancyResolved {
    return {
      discrepancyId: isSet(object.discrepancy_id) ? Long.fromValue(object.discrepancy_id) : Long.UZERO,
      vindicatedAuditor: isSet(object.vindicated_auditor) ? globalThis.String(object.vindicated_auditor) : "",
      reason: isSet(object.reason) ? discrepancyResolutionReasonFromJSON(object.reason) : 0,
      faultAttribution: isSet(object.fault_attribution) ? faultAttributionFromJSON(object.fault_attribution) : 0,
    };
  },

  toJSON(message: EventDiscrepancyResolved): unknown {
    const obj: any = {};
    if (!message.discrepancyId.equals(Long.UZERO)) {
      obj.discrepancy_id = (message.discrepancyId || Long.UZERO).toString();
    }
    if (message.vindicatedAuditor !== "") {
      obj.vindicated_auditor = message.vindicatedAuditor;
    }
    if (message.reason !== 0) {
      obj.reason = discrepancyResolutionReasonToJSON(message.reason);
    }
    if (message.faultAttribution !== 0) {
      obj.fault_attribution = faultAttributionToJSON(message.faultAttribution);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventDiscrepancyResolved>): EventDiscrepancyResolved {
    const message = createBaseEventDiscrepancyResolved();
    message.discrepancyId = (object.discrepancyId !== undefined && object.discrepancyId !== null)
      ? Long.fromValue(object.discrepancyId)
      : Long.UZERO;
    message.vindicatedAuditor = object.vindicatedAuditor ?? "";
    message.reason = object.reason ?? 0;
    message.faultAttribution = object.faultAttribution ?? 0;
    return message;
  },
};

function createBaseEventDiscrepancyTimedOut(): EventDiscrepancyTimedOut {
  return { discrepancyId: Long.UZERO, auditorA: "", auditorB: "" };
}

export const EventDiscrepancyTimedOut: MessageFns<
  EventDiscrepancyTimedOut,
  "akash.verification.v1.EventDiscrepancyTimedOut"
> = {
  $type: "akash.verification.v1.EventDiscrepancyTimedOut" as const,

  encode(message: EventDiscrepancyTimedOut, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.discrepancyId.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.discrepancyId.toString());
    }
    if (message.auditorA !== "") {
      writer.uint32(18).string(message.auditorA);
    }
    if (message.auditorB !== "") {
      writer.uint32(26).string(message.auditorB);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventDiscrepancyTimedOut {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventDiscrepancyTimedOut();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.discrepancyId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.auditorA = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.auditorB = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventDiscrepancyTimedOut {
    return {
      discrepancyId: isSet(object.discrepancy_id) ? Long.fromValue(object.discrepancy_id) : Long.UZERO,
      auditorA: isSet(object.auditor_a) ? globalThis.String(object.auditor_a) : "",
      auditorB: isSet(object.auditor_b) ? globalThis.String(object.auditor_b) : "",
    };
  },

  toJSON(message: EventDiscrepancyTimedOut): unknown {
    const obj: any = {};
    if (!message.discrepancyId.equals(Long.UZERO)) {
      obj.discrepancy_id = (message.discrepancyId || Long.UZERO).toString();
    }
    if (message.auditorA !== "") {
      obj.auditor_a = message.auditorA;
    }
    if (message.auditorB !== "") {
      obj.auditor_b = message.auditorB;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventDiscrepancyTimedOut>): EventDiscrepancyTimedOut {
    const message = createBaseEventDiscrepancyTimedOut();
    message.discrepancyId = (object.discrepancyId !== undefined && object.discrepancyId !== null)
      ? Long.fromValue(object.discrepancyId)
      : Long.UZERO;
    message.auditorA = object.auditorA ?? "";
    message.auditorB = object.auditorB ?? "";
    return message;
  },
};

function createBaseEventProviderBondPosted(): EventProviderBondPosted {
  return { provider: "", amount: undefined, totalBonded: undefined };
}

export const EventProviderBondPosted: MessageFns<
  EventProviderBondPosted,
  "akash.verification.v1.EventProviderBondPosted"
> = {
  $type: "akash.verification.v1.EventProviderBondPosted" as const,

  encode(message: EventProviderBondPosted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(18).fork()).join();
    }
    if (message.totalBonded !== undefined) {
      Coin.encode(message.totalBonded, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventProviderBondPosted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventProviderBondPosted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.totalBonded = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventProviderBondPosted {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
      totalBonded: isSet(object.total_bonded) ? Coin.fromJSON(object.total_bonded) : undefined,
    };
  },

  toJSON(message: EventProviderBondPosted): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    if (message.totalBonded !== undefined) {
      obj.total_bonded = Coin.toJSON(message.totalBonded);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventProviderBondPosted>): EventProviderBondPosted {
    const message = createBaseEventProviderBondPosted();
    message.provider = object.provider ?? "";
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    message.totalBonded = (object.totalBonded !== undefined && object.totalBonded !== null)
      ? Coin.fromPartial(object.totalBonded)
      : undefined;
    return message;
  },
};

function createBaseEventProviderBondSlashed(): EventProviderBondSlashed {
  return { provider: "", slashedAmount: undefined, reason: 0 };
}

export const EventProviderBondSlashed: MessageFns<
  EventProviderBondSlashed,
  "akash.verification.v1.EventProviderBondSlashed"
> = {
  $type: "akash.verification.v1.EventProviderBondSlashed" as const,

  encode(message: EventProviderBondSlashed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.slashedAmount !== undefined) {
      Coin.encode(message.slashedAmount, writer.uint32(18).fork()).join();
    }
    if (message.reason !== 0) {
      writer.uint32(24).int32(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventProviderBondSlashed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventProviderBondSlashed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.slashedAmount = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventProviderBondSlashed {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      slashedAmount: isSet(object.slashed_amount) ? Coin.fromJSON(object.slashed_amount) : undefined,
      reason: isSet(object.reason) ? providerBondSlashReasonFromJSON(object.reason) : 0,
    };
  },

  toJSON(message: EventProviderBondSlashed): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.slashedAmount !== undefined) {
      obj.slashed_amount = Coin.toJSON(message.slashedAmount);
    }
    if (message.reason !== 0) {
      obj.reason = providerBondSlashReasonToJSON(message.reason);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventProviderBondSlashed>): EventProviderBondSlashed {
    const message = createBaseEventProviderBondSlashed();
    message.provider = object.provider ?? "";
    message.slashedAmount = (object.slashedAmount !== undefined && object.slashedAmount !== null)
      ? Coin.fromPartial(object.slashedAmount)
      : undefined;
    message.reason = object.reason ?? 0;
    return message;
  },
};

function createBaseEventProviderBondWithdrawalInitiated(): EventProviderBondWithdrawalInitiated {
  return { provider: "", amount: undefined, completionTime: undefined };
}

export const EventProviderBondWithdrawalInitiated: MessageFns<
  EventProviderBondWithdrawalInitiated,
  "akash.verification.v1.EventProviderBondWithdrawalInitiated"
> = {
  $type: "akash.verification.v1.EventProviderBondWithdrawalInitiated" as const,

  encode(message: EventProviderBondWithdrawalInitiated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(18).fork()).join();
    }
    if (message.completionTime !== undefined) {
      Timestamp.encode(toTimestamp(message.completionTime), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventProviderBondWithdrawalInitiated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventProviderBondWithdrawalInitiated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.completionTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventProviderBondWithdrawalInitiated {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
      completionTime: isSet(object.completion_time) ? fromJsonTimestamp(object.completion_time) : undefined,
    };
  },

  toJSON(message: EventProviderBondWithdrawalInitiated): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    if (message.completionTime !== undefined) {
      obj.completion_time = message.completionTime.toISOString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventProviderBondWithdrawalInitiated>): EventProviderBondWithdrawalInitiated {
    const message = createBaseEventProviderBondWithdrawalInitiated();
    message.provider = object.provider ?? "";
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    message.completionTime = object.completionTime ?? undefined;
    return message;
  },
};

function createBaseEventProviderBondWithdrawalCompleted(): EventProviderBondWithdrawalCompleted {
  return { provider: "", amount: undefined };
}

export const EventProviderBondWithdrawalCompleted: MessageFns<
  EventProviderBondWithdrawalCompleted,
  "akash.verification.v1.EventProviderBondWithdrawalCompleted"
> = {
  $type: "akash.verification.v1.EventProviderBondWithdrawalCompleted" as const,

  encode(message: EventProviderBondWithdrawalCompleted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventProviderBondWithdrawalCompleted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventProviderBondWithdrawalCompleted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventProviderBondWithdrawalCompleted {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
    };
  },

  toJSON(message: EventProviderBondWithdrawalCompleted): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventProviderBondWithdrawalCompleted>): EventProviderBondWithdrawalCompleted {
    const message = createBaseEventProviderBondWithdrawalCompleted();
    message.provider = object.provider ?? "";
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    return message;
  },
};

function createBaseEventSnapshotHashPosted(): EventSnapshotHashPosted {
  return { provider: "", snapshotHash: new Uint8Array(0), complianceDeadline: undefined };
}

export const EventSnapshotHashPosted: MessageFns<
  EventSnapshotHashPosted,
  "akash.verification.v1.EventSnapshotHashPosted"
> = {
  $type: "akash.verification.v1.EventSnapshotHashPosted" as const,

  encode(message: EventSnapshotHashPosted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.snapshotHash.length !== 0) {
      writer.uint32(18).bytes(message.snapshotHash);
    }
    if (message.complianceDeadline !== undefined) {
      Timestamp.encode(toTimestamp(message.complianceDeadline), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventSnapshotHashPosted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventSnapshotHashPosted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.snapshotHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.complianceDeadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventSnapshotHashPosted {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      snapshotHash: isSet(object.snapshot_hash) ? bytesFromBase64(object.snapshot_hash) : new Uint8Array(0),
      complianceDeadline: isSet(object.compliance_deadline) ? fromJsonTimestamp(object.compliance_deadline) : undefined,
    };
  },

  toJSON(message: EventSnapshotHashPosted): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.snapshotHash.length !== 0) {
      obj.snapshot_hash = base64FromBytes(message.snapshotHash);
    }
    if (message.complianceDeadline !== undefined) {
      obj.compliance_deadline = message.complianceDeadline.toISOString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventSnapshotHashPosted>): EventSnapshotHashPosted {
    const message = createBaseEventSnapshotHashPosted();
    message.provider = object.provider ?? "";
    message.snapshotHash = object.snapshotHash ?? new Uint8Array(0);
    message.complianceDeadline = object.complianceDeadline ?? undefined;
    return message;
  },
};

function createBaseEventSnapshotSuspended(): EventSnapshotSuspended {
  return { provider: "" };
}

export const EventSnapshotSuspended: MessageFns<
  EventSnapshotSuspended,
  "akash.verification.v1.EventSnapshotSuspended"
> = {
  $type: "akash.verification.v1.EventSnapshotSuspended" as const,

  encode(message: EventSnapshotSuspended, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventSnapshotSuspended {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventSnapshotSuspended();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventSnapshotSuspended {
    return { provider: isSet(object.provider) ? globalThis.String(object.provider) : "" };
  },

  toJSON(message: EventSnapshotSuspended): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventSnapshotSuspended>): EventSnapshotSuspended {
    const message = createBaseEventSnapshotSuspended();
    message.provider = object.provider ?? "";
    return message;
  },
};

function createBaseEventSnapshotResumed(): EventSnapshotResumed {
  return { provider: "" };
}

export const EventSnapshotResumed: MessageFns<EventSnapshotResumed, "akash.verification.v1.EventSnapshotResumed"> = {
  $type: "akash.verification.v1.EventSnapshotResumed" as const,

  encode(message: EventSnapshotResumed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventSnapshotResumed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventSnapshotResumed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventSnapshotResumed {
    return { provider: isSet(object.provider) ? globalThis.String(object.provider) : "" };
  },

  toJSON(message: EventSnapshotResumed): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventSnapshotResumed>): EventSnapshotResumed {
    const message = createBaseEventSnapshotResumed();
    message.provider = object.provider ?? "";
    return message;
  },
};

function createBaseEventFeeEscrowed(): EventFeeEscrowed {
  return { provider: "", auditor: "", amount: undefined };
}

export const EventFeeEscrowed: MessageFns<EventFeeEscrowed, "akash.verification.v1.EventFeeEscrowed"> = {
  $type: "akash.verification.v1.EventFeeEscrowed" as const,

  encode(message: EventFeeEscrowed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.auditor !== "") {
      writer.uint32(18).string(message.auditor);
    }
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventFeeEscrowed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventFeeEscrowed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventFeeEscrowed {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
    };
  },

  toJSON(message: EventFeeEscrowed): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventFeeEscrowed>): EventFeeEscrowed {
    const message = createBaseEventFeeEscrowed();
    message.provider = object.provider ?? "";
    message.auditor = object.auditor ?? "";
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    return message;
  },
};

function createBaseEventFeeReleasedToAuditor(): EventFeeReleasedToAuditor {
  return { auditor: "", amount: undefined };
}

export const EventFeeReleasedToAuditor: MessageFns<
  EventFeeReleasedToAuditor,
  "akash.verification.v1.EventFeeReleasedToAuditor"
> = {
  $type: "akash.verification.v1.EventFeeReleasedToAuditor" as const,

  encode(message: EventFeeReleasedToAuditor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditor !== "") {
      writer.uint32(10).string(message.auditor);
    }
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventFeeReleasedToAuditor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventFeeReleasedToAuditor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventFeeReleasedToAuditor {
    return {
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
    };
  },

  toJSON(message: EventFeeReleasedToAuditor): unknown {
    const obj: any = {};
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventFeeReleasedToAuditor>): EventFeeReleasedToAuditor {
    const message = createBaseEventFeeReleasedToAuditor();
    message.auditor = object.auditor ?? "";
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    return message;
  },
};

function createBaseEventFeeReturnedToProvider(): EventFeeReturnedToProvider {
  return { provider: "", amount: undefined };
}

export const EventFeeReturnedToProvider: MessageFns<
  EventFeeReturnedToProvider,
  "akash.verification.v1.EventFeeReturnedToProvider"
> = {
  $type: "akash.verification.v1.EventFeeReturnedToProvider" as const,

  encode(message: EventFeeReturnedToProvider, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventFeeReturnedToProvider {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventFeeReturnedToProvider();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventFeeReturnedToProvider {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
    };
  },

  toJSON(message: EventFeeReturnedToProvider): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventFeeReturnedToProvider>): EventFeeReturnedToProvider {
    const message = createBaseEventFeeReturnedToProvider();
    message.provider = object.provider ?? "";
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    return message;
  },
};

function createBaseEventAuditEscrowOpened(): EventAuditEscrowOpened {
  return { auditEscrowId: Long.UZERO, provider: "", fee: undefined, providerDeposit: undefined };
}

export const EventAuditEscrowOpened: MessageFns<
  EventAuditEscrowOpened,
  "akash.verification.v1.EventAuditEscrowOpened"
> = {
  $type: "akash.verification.v1.EventAuditEscrowOpened" as const,

  encode(message: EventAuditEscrowOpened, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.auditEscrowId.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.auditEscrowId.toString());
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    if (message.fee !== undefined) {
      Coin.encode(message.fee, writer.uint32(26).fork()).join();
    }
    if (message.providerDeposit !== undefined) {
      Coin.encode(message.providerDeposit, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAuditEscrowOpened {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAuditEscrowOpened();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.auditEscrowId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fee = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.providerDeposit = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAuditEscrowOpened {
    return {
      auditEscrowId: isSet(object.audit_escrow_id) ? Long.fromValue(object.audit_escrow_id) : Long.UZERO,
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      fee: isSet(object.fee) ? Coin.fromJSON(object.fee) : undefined,
      providerDeposit: isSet(object.provider_deposit) ? Coin.fromJSON(object.provider_deposit) : undefined,
    };
  },

  toJSON(message: EventAuditEscrowOpened): unknown {
    const obj: any = {};
    if (!message.auditEscrowId.equals(Long.UZERO)) {
      obj.audit_escrow_id = (message.auditEscrowId || Long.UZERO).toString();
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.fee !== undefined) {
      obj.fee = Coin.toJSON(message.fee);
    }
    if (message.providerDeposit !== undefined) {
      obj.provider_deposit = Coin.toJSON(message.providerDeposit);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAuditEscrowOpened>): EventAuditEscrowOpened {
    const message = createBaseEventAuditEscrowOpened();
    message.auditEscrowId = (object.auditEscrowId !== undefined && object.auditEscrowId !== null)
      ? Long.fromValue(object.auditEscrowId)
      : Long.UZERO;
    message.provider = object.provider ?? "";
    message.fee = (object.fee !== undefined && object.fee !== null) ? Coin.fromPartial(object.fee) : undefined;
    message.providerDeposit = (object.providerDeposit !== undefined && object.providerDeposit !== null)
      ? Coin.fromPartial(object.providerDeposit)
      : undefined;
    return message;
  },
};

function createBaseEventAuditEscrowSettled(): EventAuditEscrowSettled {
  return { auditEscrowId: Long.UZERO, reason: 0, faultAttribution: 0 };
}

export const EventAuditEscrowSettled: MessageFns<
  EventAuditEscrowSettled,
  "akash.verification.v1.EventAuditEscrowSettled"
> = {
  $type: "akash.verification.v1.EventAuditEscrowSettled" as const,

  encode(message: EventAuditEscrowSettled, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.auditEscrowId.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.auditEscrowId.toString());
    }
    if (message.reason !== 0) {
      writer.uint32(16).int32(message.reason);
    }
    if (message.faultAttribution !== 0) {
      writer.uint32(24).int32(message.faultAttribution);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventAuditEscrowSettled {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventAuditEscrowSettled();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.auditEscrowId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.faultAttribution = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventAuditEscrowSettled {
    return {
      auditEscrowId: isSet(object.audit_escrow_id) ? Long.fromValue(object.audit_escrow_id) : Long.UZERO,
      reason: isSet(object.reason) ? auditEscrowSettlementReasonFromJSON(object.reason) : 0,
      faultAttribution: isSet(object.fault_attribution) ? faultAttributionFromJSON(object.fault_attribution) : 0,
    };
  },

  toJSON(message: EventAuditEscrowSettled): unknown {
    const obj: any = {};
    if (!message.auditEscrowId.equals(Long.UZERO)) {
      obj.audit_escrow_id = (message.auditEscrowId || Long.UZERO).toString();
    }
    if (message.reason !== 0) {
      obj.reason = auditEscrowSettlementReasonToJSON(message.reason);
    }
    if (message.faultAttribution !== 0) {
      obj.fault_attribution = faultAttributionToJSON(message.faultAttribution);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventAuditEscrowSettled>): EventAuditEscrowSettled {
    const message = createBaseEventAuditEscrowSettled();
    message.auditEscrowId = (object.auditEscrowId !== undefined && object.auditEscrowId !== null)
      ? Long.fromValue(object.auditEscrowId)
      : Long.UZERO;
    message.reason = object.reason ?? 0;
    message.faultAttribution = object.faultAttribution ?? 0;
    return message;
  },
};

function createBaseEventDepositReturnedToAuditor(): EventDepositReturnedToAuditor {
  return { auditor: "", amount: undefined };
}

export const EventDepositReturnedToAuditor: MessageFns<
  EventDepositReturnedToAuditor,
  "akash.verification.v1.EventDepositReturnedToAuditor"
> = {
  $type: "akash.verification.v1.EventDepositReturnedToAuditor" as const,

  encode(message: EventDepositReturnedToAuditor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditor !== "") {
      writer.uint32(10).string(message.auditor);
    }
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventDepositReturnedToAuditor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventDepositReturnedToAuditor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventDepositReturnedToAuditor {
    return {
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
    };
  },

  toJSON(message: EventDepositReturnedToAuditor): unknown {
    const obj: any = {};
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventDepositReturnedToAuditor>): EventDepositReturnedToAuditor {
    const message = createBaseEventDepositReturnedToAuditor();
    message.auditor = object.auditor ?? "";
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    return message;
  },
};

function createBaseEventDepositSlashed(): EventDepositSlashed {
  return { auditor: "", amount: undefined };
}

export const EventDepositSlashed: MessageFns<EventDepositSlashed, "akash.verification.v1.EventDepositSlashed"> = {
  $type: "akash.verification.v1.EventDepositSlashed" as const,

  encode(message: EventDepositSlashed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auditor !== "") {
      writer.uint32(10).string(message.auditor);
    }
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventDepositSlashed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventDepositSlashed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventDepositSlashed {
    return {
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
    };
  },

  toJSON(message: EventDepositSlashed): unknown {
    const obj: any = {};
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventDepositSlashed>): EventDepositSlashed {
    const message = createBaseEventDepositSlashed();
    message.auditor = object.auditor ?? "";
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    return message;
  },
};

function createBaseEventVerificationGraceStarted(): EventVerificationGraceStarted {
  return { graceRecordId: Long.UZERO, provider: "", preservedTier: 0 };
}

export const EventVerificationGraceStarted: MessageFns<
  EventVerificationGraceStarted,
  "akash.verification.v1.EventVerificationGraceStarted"
> = {
  $type: "akash.verification.v1.EventVerificationGraceStarted" as const,

  encode(message: EventVerificationGraceStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.graceRecordId.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.graceRecordId.toString());
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    if (message.preservedTier !== 0) {
      writer.uint32(24).int32(message.preservedTier);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventVerificationGraceStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventVerificationGraceStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.graceRecordId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.preservedTier = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventVerificationGraceStarted {
    return {
      graceRecordId: isSet(object.grace_record_id) ? Long.fromValue(object.grace_record_id) : Long.UZERO,
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      preservedTier: isSet(object.preserved_tier) ? verificationTierFromJSON(object.preserved_tier) : 0,
    };
  },

  toJSON(message: EventVerificationGraceStarted): unknown {
    const obj: any = {};
    if (!message.graceRecordId.equals(Long.UZERO)) {
      obj.grace_record_id = (message.graceRecordId || Long.UZERO).toString();
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.preservedTier !== 0) {
      obj.preserved_tier = verificationTierToJSON(message.preservedTier);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventVerificationGraceStarted>): EventVerificationGraceStarted {
    const message = createBaseEventVerificationGraceStarted();
    message.graceRecordId = (object.graceRecordId !== undefined && object.graceRecordId !== null)
      ? Long.fromValue(object.graceRecordId)
      : Long.UZERO;
    message.provider = object.provider ?? "";
    message.preservedTier = object.preservedTier ?? 0;
    return message;
  },
};

function createBaseEventVerificationGraceEnded(): EventVerificationGraceEnded {
  return { graceRecordId: Long.UZERO, status: 0 };
}

export const EventVerificationGraceEnded: MessageFns<
  EventVerificationGraceEnded,
  "akash.verification.v1.EventVerificationGraceEnded"
> = {
  $type: "akash.verification.v1.EventVerificationGraceEnded" as const,

  encode(message: EventVerificationGraceEnded, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.graceRecordId.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.graceRecordId.toString());
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventVerificationGraceEnded {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventVerificationGraceEnded();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.graceRecordId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventVerificationGraceEnded {
    return {
      graceRecordId: isSet(object.grace_record_id) ? Long.fromValue(object.grace_record_id) : Long.UZERO,
      status: isSet(object.status) ? verificationGraceStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: EventVerificationGraceEnded): unknown {
    const obj: any = {};
    if (!message.graceRecordId.equals(Long.UZERO)) {
      obj.grace_record_id = (message.graceRecordId || Long.UZERO).toString();
    }
    if (message.status !== 0) {
      obj.status = verificationGraceStatusToJSON(message.status);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventVerificationGraceEnded>): EventVerificationGraceEnded {
    const message = createBaseEventVerificationGraceEnded();
    message.graceRecordId = (object.graceRecordId !== undefined && object.graceRecordId !== null)
      ? Long.fromValue(object.graceRecordId)
      : Long.UZERO;
    message.status = object.status ?? 0;
    return message;
  },
};

function _unused_bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function _unused_base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function _unused_fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function _unused_fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function _unused_numberToLong(number: number) {
  return Long.fromNumber(number);
}

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
