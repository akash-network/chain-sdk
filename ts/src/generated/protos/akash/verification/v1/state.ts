import { bytesFromBase64, base64FromBytes, toTimestamp, fromTimestamp, fromJsonTimestamp, numberToLong, isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: akash/verification/v1/state.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";
import { Timestamp } from "../../../google/protobuf/timestamp.ts";
import {
  AttestationStatus,
  attestationStatusFromJSON,
  attestationStatusToJSON,
  AuditEscrowSettlementReason,
  auditEscrowSettlementReasonFromJSON,
  auditEscrowSettlementReasonToJSON,
  AuditEscrowStatus,
  auditEscrowStatusFromJSON,
  auditEscrowStatusToJSON,
  AuditorStatus,
  auditorStatusFromJSON,
  auditorStatusToJSON,
  BondStatus,
  bondStatusFromJSON,
  bondStatusToJSON,
  CapabilityFlag,
  capabilityFlagFromJSON,
  capabilityFlagToJSON,
  DepositStatus,
  depositStatusFromJSON,
  depositStatusToJSON,
  DiscrepancyResolutionReason,
  discrepancyResolutionReasonFromJSON,
  discrepancyResolutionReasonToJSON,
  DiscrepancyStatus,
  discrepancyStatusFromJSON,
  discrepancyStatusToJSON,
  FaultAttribution,
  faultAttributionFromJSON,
  faultAttributionToJSON,
  FeeStatus,
  feeStatusFromJSON,
  feeStatusToJSON,
  ProviderDepositStatus,
  providerDepositStatusFromJSON,
  providerDepositStatusToJSON,
  VerificationGraceStatus,
  verificationGraceStatusFromJSON,
  verificationGraceStatusToJSON,
  VerificationTier,
  verificationTierFromJSON,
  verificationTierToJSON,
  VoidedReason,
  voidedReasonFromJSON,
  voidedReasonToJSON,
} from "./types.ts";

/**
 * AuditorRecord captures the on-chain state of a registered auditor including
 * status, maximum attestation tier, bonded amount, registration and renewal
 * timestamps, discrepancy counter, and any pending bond unbonding completion.
 */
export interface AuditorRecord {
  /** Address is the bech32 account address of the auditor. */
  address: string;
  /** Status is the lifecycle status of the auditor record. */
  status: AuditorStatus;
  /**
   * MaxAttestationTier is the highest verification tier this auditor is
   * permitted to attest once the required bond has been posted.
   */
  maxAttestationTier: VerificationTier;
  /** BondAmount is the coin amount currently bonded by the auditor. */
  bondAmount:
    | Coin
    | undefined;
  /** BondStatus is the current status of the auditor bond. */
  bondStatus: BondStatus;
  /** MetadataHash is an opaque hash referencing auditor metadata (off-chain). */
  metadataHash: Uint8Array;
  /** RegisteredAt is the chain timestamp at which this auditor was registered. */
  registeredAt:
    | Date
    | undefined;
  /** RenewalDeadline is the chain timestamp by which the auditor must renew. */
  renewalDeadline:
    | Date
    | undefined;
  /**
   * DiscrepancyCount is the running count of discrepancies attributed to this
   * auditor; used by the keeper to enforce the discrepancy threshold.
   */
  discrepancyCount: Long;
  /**
   * BondUnbondingCompletionTime is the time at which a pending bond unbonding
   * completes; nil when no unbonding is in progress.
   */
  bondUnbondingCompletionTime: Date | undefined;
}

/**
 * AttestationRecord captures a single attestation issued by an auditor about
 * a provider, including the asserted tier and capabilities, the evidence
 * reference, the audit fee, lifecycle and fault state, anti-griefing
 * deposit, and the audit-escrow ID that authorized the attestation.
 */
export interface AttestationRecord {
  /** Provider is the bech32 account address of the attested provider. */
  provider: string;
  /** Auditor is the bech32 account address of the issuing auditor. */
  auditor: string;
  /** Tier is the verification tier the auditor asserts for the provider. */
  tier: VerificationTier;
  /** Capabilities is the set of capability flags the auditor asserts. */
  capabilities: CapabilityFlag[];
  /** EvidenceHash is an opaque hash referencing the auditor's evidence (off-chain). */
  evidenceHash: Uint8Array;
  /** Fee is the audit fee paid by the provider for this attestation. */
  fee:
    | Coin
    | undefined;
  /** FeeStatus is the lifecycle status of the fee held against this attestation. */
  feeStatus: FeeStatus;
  /** CreatedAt is the chain timestamp at which the attestation was created. */
  createdAt:
    | Date
    | undefined;
  /** ExpiresAt is the chain timestamp at which the attestation expires. */
  expiresAt:
    | Date
    | undefined;
  /** Status is the lifecycle status of the attestation. */
  status: AttestationStatus;
  /** VoidedReason carries the typed reason when status == Voided. */
  voidedReason: VoidedReason;
  /** Deposit is the anti-griefing deposit posted by the auditor. */
  deposit:
    | Coin
    | undefined;
  /** DepositStatus is the lifecycle status of the auditor's deposit. */
  depositStatus: DepositStatus;
  /**
   * AuditEscrowID is the identifier of the audit escrow that authorized this
   * attestation.
   */
  auditEscrowId: Long;
  /**
   * FaultAttribution carries the fault attribution recorded after settlement
   * or governance resolution involving this attestation.
   */
  faultAttribution: FaultAttribution;
}

/**
 * AuditEscrowRecord captures an audit escrow opened by a provider to fund a
 * pending attestation, including the requested tier and capabilities, the
 * audit fee and provider deposit, lifecycle status, lifecycle timestamps,
 * metadata reference, and final settlement attribution.
 */
export interface AuditEscrowRecord {
  /** ID is the unique identifier of the audit escrow. */
  id: Long;
  /** Provider is the bech32 account address of the provider that opened the escrow. */
  provider: string;
  /**
   * ConsumedByAuditor is the bech32 address of the auditor that consumed the
   * escrow when filing an attestation; empty until consumption.
   */
  consumedByAuditor: string;
  /** RequestedTier is the verification tier the provider is requesting. */
  requestedTier: VerificationTier;
  /** RequestedCapabilities is the set of capability flags the provider is requesting. */
  requestedCapabilities: CapabilityFlag[];
  /** Fee is the audit fee escrowed by the provider. */
  fee:
    | Coin
    | undefined;
  /** FeeStatus is the lifecycle status of the escrowed fee. */
  feeStatus: FeeStatus;
  /** ProviderDeposit is the deposit a provider escrows alongside the fee. */
  providerDeposit:
    | Coin
    | undefined;
  /** ProviderDepositStatus is the lifecycle status of the provider deposit. */
  providerDepositStatus: ProviderDepositStatus;
  /** Status is the lifecycle status of the audit escrow. */
  status: AuditEscrowStatus;
  /** OpenedAt is the chain timestamp at which the escrow was opened. */
  openedAt:
    | Date
    | undefined;
  /**
   * ConsumedAt is the chain timestamp at which the escrow was consumed; nil
   * until consumption.
   */
  consumedAt:
    | Date
    | undefined;
  /** ExpiresAt is the chain timestamp at which an unconsumed escrow expires. */
  expiresAt:
    | Date
    | undefined;
  /** MetadataHash is an opaque hash referencing escrow metadata (off-chain). */
  metadataHash: Uint8Array;
  /** SettlementReason is the typed reason recorded when the escrow is settled. */
  settlementReason: AuditEscrowSettlementReason;
  /** FaultAttribution is the fault attribution recorded at settlement. */
  faultAttribution: FaultAttribution;
}

/**
 * VerificationStoreRecord is a generic any-typed wrapper used by genesis
 * import/export to carry sub-record payloads addressed by their proto type URL.
 */
export interface VerificationStoreRecord {
  /** TypeURL is the fully-qualified proto type URL of the wrapped record. */
  typeUrl: string;
  /** Value is the serialized bytes of the wrapped record. */
  value: Uint8Array;
}

/**
 * DiscrepancyEvent captures a disagreement between two auditors over a
 * provider, including both attestations' asserted tiers, the resolution
 * state, pointers to the governance proposal and grace record (if any),
 * and the final resolution attribution recorded at settlement.
 */
export interface DiscrepancyEvent {
  /** ID is the unique identifier of the discrepancy event. */
  id: Long;
  /** Provider is the bech32 account address of the provider in dispute. */
  provider: string;
  /** AuditorA is the bech32 account address of the first auditor. */
  auditorA: string;
  /** AuditorATier is the tier asserted by auditor A. */
  auditorATier: VerificationTier;
  /** AuditorB is the bech32 account address of the second auditor. */
  auditorB: string;
  /** AuditorBTier is the tier asserted by auditor B. */
  auditorBTier: VerificationTier;
  /** Timestamp is the chain time at which the discrepancy was detected. */
  timestamp:
    | Date
    | undefined;
  /** ResolutionStatus is the lifecycle status of the resolution flow. */
  resolutionStatus: DiscrepancyStatus;
  /**
   * ResolutionProposalID is the governance proposal that resolved the
   * discrepancy; zero when no proposal has been filed.
   */
  resolutionProposalId: Long;
  /**
   * GraceRecordID is the provider verification grace record opened in response
   * to this discrepancy; zero when no grace record exists.
   */
  graceRecordId: Long;
  /** ResolutionReason is the typed reason recorded at resolution. */
  resolutionReason: DiscrepancyResolutionReason;
  /** FaultAttribution is the fault attribution recorded at resolution. */
  faultAttribution: FaultAttribution;
  /**
   * ResolutionEvidenceHash is an opaque hash referencing the evidence used at
   * resolution (off-chain).
   */
  resolutionEvidenceHash: Uint8Array;
}

/**
 * ProviderVerificationGraceRecord preserves a provider's verification tier
 * across a discrepancy by tracking the source discrepancies, preserved tier,
 * and the grace window during which the provider may continue to operate.
 */
export interface ProviderVerificationGraceRecord {
  /** ID is the unique identifier of the grace record. */
  id: Long;
  /** Provider is the bech32 account address of the provider in grace. */
  provider: string;
  /** PreservedTier is the verification tier preserved for the provider during grace. */
  preservedTier: VerificationTier;
  /** SourceDiscrepancyIDs is the list of discrepancy IDs that opened this grace. */
  sourceDiscrepancyIds: Long[];
  /** StartedAt is the chain timestamp at which the grace window began. */
  startedAt:
    | Date
    | undefined;
  /** ExpiresAt is the chain timestamp at which the grace window expires. */
  expiresAt:
    | Date
    | undefined;
  /** Status is the lifecycle status of the grace window. */
  status: VerificationGraceStatus;
}

/**
 * ProviderBondRecord holds a provider's resource-scaled verification bond,
 * any unbonding entries currently in progress, and slash history.
 */
export interface ProviderBondRecord {
  /** Provider is the bech32 account address of the bonded provider. */
  provider: string;
  /** BondedAmount is the coin amount currently bonded by the provider. */
  bondedAmount:
    | Coin
    | undefined;
  /** UnbondingEntries is the list of pending unbondings against this bond. */
  unbondingEntries: UnbondingEntry[];
  /** Slashed indicates whether this provider bond has ever been slashed. */
  slashed: boolean;
  /**
   * LastSlashTime is the chain timestamp of the most recent slash; nil if
   * the bond has never been slashed.
   */
  lastSlashTime: Date | undefined;
}

/**
 * UnbondingEntry represents a single bond-unbonding amount and completion
 * time queued against a provider or auditor bond.
 */
export interface UnbondingEntry {
  /** Amount is the coin amount queued for unbonding. */
  amount:
    | Coin
    | undefined;
  /**
   * CompletionTime is the chain timestamp at which this entry completes
   * unbonding and the amount becomes spendable.
   */
  completionTime: Date | undefined;
}

/**
 * SoftwareIdentity carries release artifact identity and signature metadata.
 * Providers post these fields as evidence; auditors verify them off-chain
 * against the published Akash release key.
 */
export interface SoftwareIdentity {
  /** Version is the provider or inventory software version string. */
  version: string;
  /** ArtifactRef identifies the release artifact whose digest/signature is reported. */
  artifactRef: string;
  /** DigestAlgorithm identifies the digest algorithm, e.g. sha3-256. */
  digestAlgorithm: string;
  /** Digest is the release artifact digest bytes. */
  digest: Uint8Array;
  /** SignatureType identifies the signature format, e.g. cosign. */
  signatureType: string;
  /** Signature is the detached signature bytes when carried inline. */
  signature: Uint8Array;
  /** SignatureRef identifies an external signature or bundle. */
  signatureRef: string;
  /** PublicKeyRef identifies the published release public key. */
  publicKeyRef: string;
}

/**
 * ResourceSummary captures the resource counts a provider claims at snapshot
 * time. Used by the chain to scale the provider bond requirement and as
 * evidence in snapshot records. Software identity fields are evidence-only and
 * do not drive on-chain enforcement.
 */
export interface ResourceSummary {
  /** TotalGPUs is the total number of GPUs the provider claims. */
  totalGpus: number;
  /** TotalVCPUs is the total number of virtual CPUs the provider claims. */
  totalVcpus: number;
  /** TotalMemoryMB is the total memory in megabytes the provider claims. */
  totalMemoryMb: Long;
  /** TotalStorageMB is the total storage in megabytes the provider claims. */
  totalStorageMb: Long;
  /** ActiveLeases is the number of leases currently active on the provider. */
  activeLeases: number;
  /** SoftwareVersion is the provider software version string kept for compatibility. */
  softwareVersion: string;
  /** SoftwareSignature is the provider software signature kept for compatibility. */
  softwareSignature: Uint8Array;
  /** SoftwareIdentity carries structured release artifact metadata. */
  softwareIdentity: SoftwareIdentity | undefined;
}

/**
 * ProviderSnapshotRecord is the on-chain record of a provider's most recent
 * resource snapshot, including the snapshot hash, resource summary, posting
 * and snapshot timestamps, the compliance deadline, and suspension state.
 */
export interface ProviderSnapshotRecord {
  /** Provider is the bech32 account address of the snapshotting provider. */
  provider: string;
  /** SnapshotHash is the opaque hash of the snapshot payload (off-chain). */
  snapshotHash: Uint8Array;
  /** ResourceSummary is the inline resource counts the provider claims at snapshot time. */
  resourceSummary:
    | ResourceSummary
    | undefined;
  /** PostedAt is the chain timestamp at which the snapshot record was posted on-chain. */
  postedAt:
    | Date
    | undefined;
  /** SnapshotTimestamp is the timestamp embedded by the provider in the snapshot itself. */
  snapshotTimestamp:
    | Date
    | undefined;
  /**
   * ComplianceDeadline is the chain timestamp by which the next snapshot must be posted
   * to remain compliant with the snapshot cadence.
   */
  complianceDeadline:
    | Date
    | undefined;
  /**
   * Suspended indicates whether this provider snapshot has been suspended for
   * missing the compliance deadline.
   */
  suspended: boolean;
}

function createBaseAuditorRecord(): AuditorRecord {
  return {
    address: "",
    status: 0,
    maxAttestationTier: 0,
    bondAmount: undefined,
    bondStatus: 0,
    metadataHash: new Uint8Array(0),
    registeredAt: undefined,
    renewalDeadline: undefined,
    discrepancyCount: Long.UZERO,
    bondUnbondingCompletionTime: undefined,
  };
}

export const AuditorRecord: MessageFns<AuditorRecord, "akash.verification.v1.AuditorRecord"> = {
  $type: "akash.verification.v1.AuditorRecord" as const,

  encode(message: AuditorRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.maxAttestationTier !== 0) {
      writer.uint32(24).int32(message.maxAttestationTier);
    }
    if (message.bondAmount !== undefined) {
      Coin.encode(message.bondAmount, writer.uint32(34).fork()).join();
    }
    if (message.bondStatus !== 0) {
      writer.uint32(40).int32(message.bondStatus);
    }
    if (message.metadataHash.length !== 0) {
      writer.uint32(50).bytes(message.metadataHash);
    }
    if (message.registeredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.registeredAt), writer.uint32(58).fork()).join();
    }
    if (message.renewalDeadline !== undefined) {
      Timestamp.encode(toTimestamp(message.renewalDeadline), writer.uint32(66).fork()).join();
    }
    if (!message.discrepancyCount.equals(Long.UZERO)) {
      writer.uint32(72).uint64(message.discrepancyCount.toString());
    }
    if (message.bondUnbondingCompletionTime !== undefined) {
      Timestamp.encode(toTimestamp(message.bondUnbondingCompletionTime), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuditorRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditorRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxAttestationTier = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.bondAmount = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.bondStatus = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.metadataHash = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.registeredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.renewalDeadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.discrepancyCount = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.bondUnbondingCompletionTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditorRecord {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      status: isSet(object.status) ? auditorStatusFromJSON(object.status) : 0,
      maxAttestationTier: isSet(object.max_attestation_tier)
        ? verificationTierFromJSON(object.max_attestation_tier)
        : 0,
      bondAmount: isSet(object.bond_amount) ? Coin.fromJSON(object.bond_amount) : undefined,
      bondStatus: isSet(object.bond_status) ? bondStatusFromJSON(object.bond_status) : 0,
      metadataHash: isSet(object.metadata_hash) ? bytesFromBase64(object.metadata_hash) : new Uint8Array(0),
      registeredAt: isSet(object.registered_at) ? fromJsonTimestamp(object.registered_at) : undefined,
      renewalDeadline: isSet(object.renewal_deadline) ? fromJsonTimestamp(object.renewal_deadline) : undefined,
      discrepancyCount: isSet(object.discrepancy_count) ? Long.fromValue(object.discrepancy_count) : Long.UZERO,
      bondUnbondingCompletionTime: isSet(object.bond_unbonding_completion_time)
        ? fromJsonTimestamp(object.bond_unbonding_completion_time)
        : undefined,
    };
  },

  toJSON(message: AuditorRecord): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.status !== 0) {
      obj.status = auditorStatusToJSON(message.status);
    }
    if (message.maxAttestationTier !== 0) {
      obj.max_attestation_tier = verificationTierToJSON(message.maxAttestationTier);
    }
    if (message.bondAmount !== undefined) {
      obj.bond_amount = Coin.toJSON(message.bondAmount);
    }
    if (message.bondStatus !== 0) {
      obj.bond_status = bondStatusToJSON(message.bondStatus);
    }
    if (message.metadataHash.length !== 0) {
      obj.metadata_hash = base64FromBytes(message.metadataHash);
    }
    if (message.registeredAt !== undefined) {
      obj.registered_at = message.registeredAt.toISOString();
    }
    if (message.renewalDeadline !== undefined) {
      obj.renewal_deadline = message.renewalDeadline.toISOString();
    }
    if (!message.discrepancyCount.equals(Long.UZERO)) {
      obj.discrepancy_count = (message.discrepancyCount || Long.UZERO).toString();
    }
    if (message.bondUnbondingCompletionTime !== undefined) {
      obj.bond_unbonding_completion_time = message.bondUnbondingCompletionTime.toISOString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<AuditorRecord>): AuditorRecord {
    const message = createBaseAuditorRecord();
    message.address = object.address ?? "";
    message.status = object.status ?? 0;
    message.maxAttestationTier = object.maxAttestationTier ?? 0;
    message.bondAmount = (object.bondAmount !== undefined && object.bondAmount !== null)
      ? Coin.fromPartial(object.bondAmount)
      : undefined;
    message.bondStatus = object.bondStatus ?? 0;
    message.metadataHash = object.metadataHash ?? new Uint8Array(0);
    message.registeredAt = object.registeredAt ?? undefined;
    message.renewalDeadline = object.renewalDeadline ?? undefined;
    message.discrepancyCount = (object.discrepancyCount !== undefined && object.discrepancyCount !== null)
      ? Long.fromValue(object.discrepancyCount)
      : Long.UZERO;
    message.bondUnbondingCompletionTime = object.bondUnbondingCompletionTime ?? undefined;
    return message;
  },
};

function createBaseAttestationRecord(): AttestationRecord {
  return {
    provider: "",
    auditor: "",
    tier: 0,
    capabilities: [],
    evidenceHash: new Uint8Array(0),
    fee: undefined,
    feeStatus: 0,
    createdAt: undefined,
    expiresAt: undefined,
    status: 0,
    voidedReason: 0,
    deposit: undefined,
    depositStatus: 0,
    auditEscrowId: Long.UZERO,
    faultAttribution: 0,
  };
}

export const AttestationRecord: MessageFns<AttestationRecord, "akash.verification.v1.AttestationRecord"> = {
  $type: "akash.verification.v1.AttestationRecord" as const,

  encode(message: AttestationRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.auditor !== "") {
      writer.uint32(18).string(message.auditor);
    }
    if (message.tier !== 0) {
      writer.uint32(24).int32(message.tier);
    }
    for (const v of message.capabilities) {
      writer.uint32(32).int32(v!);
    }
    if (message.evidenceHash.length !== 0) {
      writer.uint32(42).bytes(message.evidenceHash);
    }
    if (message.fee !== undefined) {
      Coin.encode(message.fee, writer.uint32(50).fork()).join();
    }
    if (message.feeStatus !== 0) {
      writer.uint32(56).int32(message.feeStatus);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(66).fork()).join();
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(74).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(80).int32(message.status);
    }
    if (message.voidedReason !== 0) {
      writer.uint32(88).int32(message.voidedReason);
    }
    if (message.deposit !== undefined) {
      Coin.encode(message.deposit, writer.uint32(98).fork()).join();
    }
    if (message.depositStatus !== 0) {
      writer.uint32(104).int32(message.depositStatus);
    }
    if (!message.auditEscrowId.equals(Long.UZERO)) {
      writer.uint32(112).uint64(message.auditEscrowId.toString());
    }
    if (message.faultAttribution !== 0) {
      writer.uint32(120).int32(message.faultAttribution);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttestationRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttestationRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.auditor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tier = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.capabilities.push(reader.int32() as any);

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.capabilities.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.evidenceHash = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.fee = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.feeStatus = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.voidedReason = reader.int32() as any;
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.deposit = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.depositStatus = reader.int32() as any;
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.auditEscrowId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.faultAttribution = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttestationRecord {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      auditor: isSet(object.auditor) ? globalThis.String(object.auditor) : "",
      tier: isSet(object.tier) ? verificationTierFromJSON(object.tier) : 0,
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => capabilityFlagFromJSON(e))
        : [],
      evidenceHash: isSet(object.evidence_hash) ? bytesFromBase64(object.evidence_hash) : new Uint8Array(0),
      fee: isSet(object.fee) ? Coin.fromJSON(object.fee) : undefined,
      feeStatus: isSet(object.fee_status) ? feeStatusFromJSON(object.fee_status) : 0,
      createdAt: isSet(object.created_at) ? fromJsonTimestamp(object.created_at) : undefined,
      expiresAt: isSet(object.expires_at) ? fromJsonTimestamp(object.expires_at) : undefined,
      status: isSet(object.status) ? attestationStatusFromJSON(object.status) : 0,
      voidedReason: isSet(object.voided_reason) ? voidedReasonFromJSON(object.voided_reason) : 0,
      deposit: isSet(object.deposit) ? Coin.fromJSON(object.deposit) : undefined,
      depositStatus: isSet(object.deposit_status) ? depositStatusFromJSON(object.deposit_status) : 0,
      auditEscrowId: isSet(object.audit_escrow_id) ? Long.fromValue(object.audit_escrow_id) : Long.UZERO,
      faultAttribution: isSet(object.fault_attribution) ? faultAttributionFromJSON(object.fault_attribution) : 0,
    };
  },

  toJSON(message: AttestationRecord): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.auditor !== "") {
      obj.auditor = message.auditor;
    }
    if (message.tier !== 0) {
      obj.tier = verificationTierToJSON(message.tier);
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => capabilityFlagToJSON(e));
    }
    if (message.evidenceHash.length !== 0) {
      obj.evidence_hash = base64FromBytes(message.evidenceHash);
    }
    if (message.fee !== undefined) {
      obj.fee = Coin.toJSON(message.fee);
    }
    if (message.feeStatus !== 0) {
      obj.fee_status = feeStatusToJSON(message.feeStatus);
    }
    if (message.createdAt !== undefined) {
      obj.created_at = message.createdAt.toISOString();
    }
    if (message.expiresAt !== undefined) {
      obj.expires_at = message.expiresAt.toISOString();
    }
    if (message.status !== 0) {
      obj.status = attestationStatusToJSON(message.status);
    }
    if (message.voidedReason !== 0) {
      obj.voided_reason = voidedReasonToJSON(message.voidedReason);
    }
    if (message.deposit !== undefined) {
      obj.deposit = Coin.toJSON(message.deposit);
    }
    if (message.depositStatus !== 0) {
      obj.deposit_status = depositStatusToJSON(message.depositStatus);
    }
    if (!message.auditEscrowId.equals(Long.UZERO)) {
      obj.audit_escrow_id = (message.auditEscrowId || Long.UZERO).toString();
    }
    if (message.faultAttribution !== 0) {
      obj.fault_attribution = faultAttributionToJSON(message.faultAttribution);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<AttestationRecord>): AttestationRecord {
    const message = createBaseAttestationRecord();
    message.provider = object.provider ?? "";
    message.auditor = object.auditor ?? "";
    message.tier = object.tier ?? 0;
    message.capabilities = object.capabilities?.map((e) => e) || [];
    message.evidenceHash = object.evidenceHash ?? new Uint8Array(0);
    message.fee = (object.fee !== undefined && object.fee !== null) ? Coin.fromPartial(object.fee) : undefined;
    message.feeStatus = object.feeStatus ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.expiresAt = object.expiresAt ?? undefined;
    message.status = object.status ?? 0;
    message.voidedReason = object.voidedReason ?? 0;
    message.deposit = (object.deposit !== undefined && object.deposit !== null)
      ? Coin.fromPartial(object.deposit)
      : undefined;
    message.depositStatus = object.depositStatus ?? 0;
    message.auditEscrowId = (object.auditEscrowId !== undefined && object.auditEscrowId !== null)
      ? Long.fromValue(object.auditEscrowId)
      : Long.UZERO;
    message.faultAttribution = object.faultAttribution ?? 0;
    return message;
  },
};

function createBaseAuditEscrowRecord(): AuditEscrowRecord {
  return {
    id: Long.UZERO,
    provider: "",
    consumedByAuditor: "",
    requestedTier: 0,
    requestedCapabilities: [],
    fee: undefined,
    feeStatus: 0,
    providerDeposit: undefined,
    providerDepositStatus: 0,
    status: 0,
    openedAt: undefined,
    consumedAt: undefined,
    expiresAt: undefined,
    metadataHash: new Uint8Array(0),
    settlementReason: 0,
    faultAttribution: 0,
  };
}

export const AuditEscrowRecord: MessageFns<AuditEscrowRecord, "akash.verification.v1.AuditEscrowRecord"> = {
  $type: "akash.verification.v1.AuditEscrowRecord" as const,

  encode(message: AuditEscrowRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.id.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.id.toString());
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    if (message.consumedByAuditor !== "") {
      writer.uint32(26).string(message.consumedByAuditor);
    }
    if (message.requestedTier !== 0) {
      writer.uint32(32).int32(message.requestedTier);
    }
    for (const v of message.requestedCapabilities) {
      writer.uint32(40).int32(v!);
    }
    if (message.fee !== undefined) {
      Coin.encode(message.fee, writer.uint32(50).fork()).join();
    }
    if (message.feeStatus !== 0) {
      writer.uint32(56).int32(message.feeStatus);
    }
    if (message.providerDeposit !== undefined) {
      Coin.encode(message.providerDeposit, writer.uint32(66).fork()).join();
    }
    if (message.providerDepositStatus !== 0) {
      writer.uint32(72).int32(message.providerDepositStatus);
    }
    if (message.status !== 0) {
      writer.uint32(80).int32(message.status);
    }
    if (message.openedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.openedAt), writer.uint32(90).fork()).join();
    }
    if (message.consumedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.consumedAt), writer.uint32(98).fork()).join();
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(106).fork()).join();
    }
    if (message.metadataHash.length !== 0) {
      writer.uint32(114).bytes(message.metadataHash);
    }
    if (message.settlementReason !== 0) {
      writer.uint32(120).int32(message.settlementReason);
    }
    if (message.faultAttribution !== 0) {
      writer.uint32(128).int32(message.faultAttribution);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuditEscrowRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditEscrowRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.consumedByAuditor = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.requestedTier = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.requestedCapabilities.push(reader.int32() as any);

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.requestedCapabilities.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.fee = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.feeStatus = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.providerDeposit = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.providerDepositStatus = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.openedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.consumedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.metadataHash = reader.bytes();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.settlementReason = reader.int32() as any;
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.faultAttribution = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditEscrowRecord {
    return {
      id: isSet(object.id) ? Long.fromValue(object.id) : Long.UZERO,
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      consumedByAuditor: isSet(object.consumed_by_auditor) ? globalThis.String(object.consumed_by_auditor) : "",
      requestedTier: isSet(object.requested_tier) ? verificationTierFromJSON(object.requested_tier) : 0,
      requestedCapabilities: globalThis.Array.isArray(object?.requested_capabilities)
        ? object.requested_capabilities.map((e: any) => capabilityFlagFromJSON(e))
        : [],
      fee: isSet(object.fee) ? Coin.fromJSON(object.fee) : undefined,
      feeStatus: isSet(object.fee_status) ? feeStatusFromJSON(object.fee_status) : 0,
      providerDeposit: isSet(object.provider_deposit) ? Coin.fromJSON(object.provider_deposit) : undefined,
      providerDepositStatus: isSet(object.provider_deposit_status)
        ? providerDepositStatusFromJSON(object.provider_deposit_status)
        : 0,
      status: isSet(object.status) ? auditEscrowStatusFromJSON(object.status) : 0,
      openedAt: isSet(object.opened_at) ? fromJsonTimestamp(object.opened_at) : undefined,
      consumedAt: isSet(object.consumed_at) ? fromJsonTimestamp(object.consumed_at) : undefined,
      expiresAt: isSet(object.expires_at) ? fromJsonTimestamp(object.expires_at) : undefined,
      metadataHash: isSet(object.metadata_hash) ? bytesFromBase64(object.metadata_hash) : new Uint8Array(0),
      settlementReason: isSet(object.settlement_reason)
        ? auditEscrowSettlementReasonFromJSON(object.settlement_reason)
        : 0,
      faultAttribution: isSet(object.fault_attribution) ? faultAttributionFromJSON(object.fault_attribution) : 0,
    };
  },

  toJSON(message: AuditEscrowRecord): unknown {
    const obj: any = {};
    if (!message.id.equals(Long.UZERO)) {
      obj.id = (message.id || Long.UZERO).toString();
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.consumedByAuditor !== "") {
      obj.consumed_by_auditor = message.consumedByAuditor;
    }
    if (message.requestedTier !== 0) {
      obj.requested_tier = verificationTierToJSON(message.requestedTier);
    }
    if (message.requestedCapabilities?.length) {
      obj.requested_capabilities = message.requestedCapabilities.map((e) => capabilityFlagToJSON(e));
    }
    if (message.fee !== undefined) {
      obj.fee = Coin.toJSON(message.fee);
    }
    if (message.feeStatus !== 0) {
      obj.fee_status = feeStatusToJSON(message.feeStatus);
    }
    if (message.providerDeposit !== undefined) {
      obj.provider_deposit = Coin.toJSON(message.providerDeposit);
    }
    if (message.providerDepositStatus !== 0) {
      obj.provider_deposit_status = providerDepositStatusToJSON(message.providerDepositStatus);
    }
    if (message.status !== 0) {
      obj.status = auditEscrowStatusToJSON(message.status);
    }
    if (message.openedAt !== undefined) {
      obj.opened_at = message.openedAt.toISOString();
    }
    if (message.consumedAt !== undefined) {
      obj.consumed_at = message.consumedAt.toISOString();
    }
    if (message.expiresAt !== undefined) {
      obj.expires_at = message.expiresAt.toISOString();
    }
    if (message.metadataHash.length !== 0) {
      obj.metadata_hash = base64FromBytes(message.metadataHash);
    }
    if (message.settlementReason !== 0) {
      obj.settlement_reason = auditEscrowSettlementReasonToJSON(message.settlementReason);
    }
    if (message.faultAttribution !== 0) {
      obj.fault_attribution = faultAttributionToJSON(message.faultAttribution);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<AuditEscrowRecord>): AuditEscrowRecord {
    const message = createBaseAuditEscrowRecord();
    message.id = (object.id !== undefined && object.id !== null) ? Long.fromValue(object.id) : Long.UZERO;
    message.provider = object.provider ?? "";
    message.consumedByAuditor = object.consumedByAuditor ?? "";
    message.requestedTier = object.requestedTier ?? 0;
    message.requestedCapabilities = object.requestedCapabilities?.map((e) => e) || [];
    message.fee = (object.fee !== undefined && object.fee !== null) ? Coin.fromPartial(object.fee) : undefined;
    message.feeStatus = object.feeStatus ?? 0;
    message.providerDeposit = (object.providerDeposit !== undefined && object.providerDeposit !== null)
      ? Coin.fromPartial(object.providerDeposit)
      : undefined;
    message.providerDepositStatus = object.providerDepositStatus ?? 0;
    message.status = object.status ?? 0;
    message.openedAt = object.openedAt ?? undefined;
    message.consumedAt = object.consumedAt ?? undefined;
    message.expiresAt = object.expiresAt ?? undefined;
    message.metadataHash = object.metadataHash ?? new Uint8Array(0);
    message.settlementReason = object.settlementReason ?? 0;
    message.faultAttribution = object.faultAttribution ?? 0;
    return message;
  },
};

function createBaseVerificationStoreRecord(): VerificationStoreRecord {
  return { typeUrl: "", value: new Uint8Array(0) };
}

export const VerificationStoreRecord: MessageFns<
  VerificationStoreRecord,
  "akash.verification.v1.VerificationStoreRecord"
> = {
  $type: "akash.verification.v1.VerificationStoreRecord" as const,

  encode(message: VerificationStoreRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.typeUrl !== "") {
      writer.uint32(10).string(message.typeUrl);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerificationStoreRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerificationStoreRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.typeUrl = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerificationStoreRecord {
    return {
      typeUrl: isSet(object.type_url) ? globalThis.String(object.type_url) : "",
      value: isSet(object.value) ? bytesFromBase64(object.value) : new Uint8Array(0),
    };
  },

  toJSON(message: VerificationStoreRecord): unknown {
    const obj: any = {};
    if (message.typeUrl !== "") {
      obj.type_url = message.typeUrl;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<VerificationStoreRecord>): VerificationStoreRecord {
    const message = createBaseVerificationStoreRecord();
    message.typeUrl = object.typeUrl ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  },
};

function createBaseDiscrepancyEvent(): DiscrepancyEvent {
  return {
    id: Long.UZERO,
    provider: "",
    auditorA: "",
    auditorATier: 0,
    auditorB: "",
    auditorBTier: 0,
    timestamp: undefined,
    resolutionStatus: 0,
    resolutionProposalId: Long.UZERO,
    graceRecordId: Long.UZERO,
    resolutionReason: 0,
    faultAttribution: 0,
    resolutionEvidenceHash: new Uint8Array(0),
  };
}

export const DiscrepancyEvent: MessageFns<DiscrepancyEvent, "akash.verification.v1.DiscrepancyEvent"> = {
  $type: "akash.verification.v1.DiscrepancyEvent" as const,

  encode(message: DiscrepancyEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.id.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.id.toString());
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    if (message.auditorA !== "") {
      writer.uint32(26).string(message.auditorA);
    }
    if (message.auditorATier !== 0) {
      writer.uint32(32).int32(message.auditorATier);
    }
    if (message.auditorB !== "") {
      writer.uint32(42).string(message.auditorB);
    }
    if (message.auditorBTier !== 0) {
      writer.uint32(48).int32(message.auditorBTier);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(58).fork()).join();
    }
    if (message.resolutionStatus !== 0) {
      writer.uint32(64).int32(message.resolutionStatus);
    }
    if (!message.resolutionProposalId.equals(Long.UZERO)) {
      writer.uint32(72).uint64(message.resolutionProposalId.toString());
    }
    if (!message.graceRecordId.equals(Long.UZERO)) {
      writer.uint32(80).uint64(message.graceRecordId.toString());
    }
    if (message.resolutionReason !== 0) {
      writer.uint32(88).int32(message.resolutionReason);
    }
    if (message.faultAttribution !== 0) {
      writer.uint32(96).int32(message.faultAttribution);
    }
    if (message.resolutionEvidenceHash.length !== 0) {
      writer.uint32(106).bytes(message.resolutionEvidenceHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscrepancyEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscrepancyEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.auditorA = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.auditorATier = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.auditorB = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.auditorBTier = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.resolutionStatus = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.resolutionProposalId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.graceRecordId = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.resolutionReason = reader.int32() as any;
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.faultAttribution = reader.int32() as any;
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.resolutionEvidenceHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscrepancyEvent {
    return {
      id: isSet(object.id) ? Long.fromValue(object.id) : Long.UZERO,
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      auditorA: isSet(object.auditor_a) ? globalThis.String(object.auditor_a) : "",
      auditorATier: isSet(object.auditor_a_tier) ? verificationTierFromJSON(object.auditor_a_tier) : 0,
      auditorB: isSet(object.auditor_b) ? globalThis.String(object.auditor_b) : "",
      auditorBTier: isSet(object.auditor_b_tier) ? verificationTierFromJSON(object.auditor_b_tier) : 0,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      resolutionStatus: isSet(object.resolution_status) ? discrepancyStatusFromJSON(object.resolution_status) : 0,
      resolutionProposalId: isSet(object.resolution_proposal_id)
        ? Long.fromValue(object.resolution_proposal_id)
        : Long.UZERO,
      graceRecordId: isSet(object.grace_record_id) ? Long.fromValue(object.grace_record_id) : Long.UZERO,
      resolutionReason: isSet(object.resolution_reason)
        ? discrepancyResolutionReasonFromJSON(object.resolution_reason)
        : 0,
      faultAttribution: isSet(object.fault_attribution) ? faultAttributionFromJSON(object.fault_attribution) : 0,
      resolutionEvidenceHash: isSet(object.resolution_evidence_hash)
        ? bytesFromBase64(object.resolution_evidence_hash)
        : new Uint8Array(0),
    };
  },

  toJSON(message: DiscrepancyEvent): unknown {
    const obj: any = {};
    if (!message.id.equals(Long.UZERO)) {
      obj.id = (message.id || Long.UZERO).toString();
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.auditorA !== "") {
      obj.auditor_a = message.auditorA;
    }
    if (message.auditorATier !== 0) {
      obj.auditor_a_tier = verificationTierToJSON(message.auditorATier);
    }
    if (message.auditorB !== "") {
      obj.auditor_b = message.auditorB;
    }
    if (message.auditorBTier !== 0) {
      obj.auditor_b_tier = verificationTierToJSON(message.auditorBTier);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.resolutionStatus !== 0) {
      obj.resolution_status = discrepancyStatusToJSON(message.resolutionStatus);
    }
    if (!message.resolutionProposalId.equals(Long.UZERO)) {
      obj.resolution_proposal_id = (message.resolutionProposalId || Long.UZERO).toString();
    }
    if (!message.graceRecordId.equals(Long.UZERO)) {
      obj.grace_record_id = (message.graceRecordId || Long.UZERO).toString();
    }
    if (message.resolutionReason !== 0) {
      obj.resolution_reason = discrepancyResolutionReasonToJSON(message.resolutionReason);
    }
    if (message.faultAttribution !== 0) {
      obj.fault_attribution = faultAttributionToJSON(message.faultAttribution);
    }
    if (message.resolutionEvidenceHash.length !== 0) {
      obj.resolution_evidence_hash = base64FromBytes(message.resolutionEvidenceHash);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<DiscrepancyEvent>): DiscrepancyEvent {
    const message = createBaseDiscrepancyEvent();
    message.id = (object.id !== undefined && object.id !== null) ? Long.fromValue(object.id) : Long.UZERO;
    message.provider = object.provider ?? "";
    message.auditorA = object.auditorA ?? "";
    message.auditorATier = object.auditorATier ?? 0;
    message.auditorB = object.auditorB ?? "";
    message.auditorBTier = object.auditorBTier ?? 0;
    message.timestamp = object.timestamp ?? undefined;
    message.resolutionStatus = object.resolutionStatus ?? 0;
    message.resolutionProposalId = (object.resolutionProposalId !== undefined && object.resolutionProposalId !== null)
      ? Long.fromValue(object.resolutionProposalId)
      : Long.UZERO;
    message.graceRecordId = (object.graceRecordId !== undefined && object.graceRecordId !== null)
      ? Long.fromValue(object.graceRecordId)
      : Long.UZERO;
    message.resolutionReason = object.resolutionReason ?? 0;
    message.faultAttribution = object.faultAttribution ?? 0;
    message.resolutionEvidenceHash = object.resolutionEvidenceHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseProviderVerificationGraceRecord(): ProviderVerificationGraceRecord {
  return {
    id: Long.UZERO,
    provider: "",
    preservedTier: 0,
    sourceDiscrepancyIds: [],
    startedAt: undefined,
    expiresAt: undefined,
    status: 0,
  };
}

export const ProviderVerificationGraceRecord: MessageFns<
  ProviderVerificationGraceRecord,
  "akash.verification.v1.ProviderVerificationGraceRecord"
> = {
  $type: "akash.verification.v1.ProviderVerificationGraceRecord" as const,

  encode(message: ProviderVerificationGraceRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.id.equals(Long.UZERO)) {
      writer.uint32(8).uint64(message.id.toString());
    }
    if (message.provider !== "") {
      writer.uint32(18).string(message.provider);
    }
    if (message.preservedTier !== 0) {
      writer.uint32(24).int32(message.preservedTier);
    }
    for (const v of message.sourceDiscrepancyIds) {
      writer.uint32(32).uint64(v!.toString());
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(42).fork()).join();
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(50).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderVerificationGraceRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderVerificationGraceRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.preservedTier = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.sourceDiscrepancyIds.push(Long.fromString(reader.uint64().toString(), true));

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.sourceDiscrepancyIds.push(Long.fromString(reader.uint64().toString(), true));
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderVerificationGraceRecord {
    return {
      id: isSet(object.id) ? Long.fromValue(object.id) : Long.UZERO,
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      preservedTier: isSet(object.preserved_tier) ? verificationTierFromJSON(object.preserved_tier) : 0,
      sourceDiscrepancyIds: globalThis.Array.isArray(object?.source_discrepancy_ids)
        ? object.source_discrepancy_ids.map((e: any) => Long.fromValue(e))
        : [],
      startedAt: isSet(object.started_at) ? fromJsonTimestamp(object.started_at) : undefined,
      expiresAt: isSet(object.expires_at) ? fromJsonTimestamp(object.expires_at) : undefined,
      status: isSet(object.status) ? verificationGraceStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: ProviderVerificationGraceRecord): unknown {
    const obj: any = {};
    if (!message.id.equals(Long.UZERO)) {
      obj.id = (message.id || Long.UZERO).toString();
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.preservedTier !== 0) {
      obj.preserved_tier = verificationTierToJSON(message.preservedTier);
    }
    if (message.sourceDiscrepancyIds?.length) {
      obj.source_discrepancy_ids = message.sourceDiscrepancyIds.map((e) => (e || Long.UZERO).toString());
    }
    if (message.startedAt !== undefined) {
      obj.started_at = message.startedAt.toISOString();
    }
    if (message.expiresAt !== undefined) {
      obj.expires_at = message.expiresAt.toISOString();
    }
    if (message.status !== 0) {
      obj.status = verificationGraceStatusToJSON(message.status);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<ProviderVerificationGraceRecord>): ProviderVerificationGraceRecord {
    const message = createBaseProviderVerificationGraceRecord();
    message.id = (object.id !== undefined && object.id !== null) ? Long.fromValue(object.id) : Long.UZERO;
    message.provider = object.provider ?? "";
    message.preservedTier = object.preservedTier ?? 0;
    message.sourceDiscrepancyIds = object.sourceDiscrepancyIds?.map((e) => Long.fromValue(e)) || [];
    message.startedAt = object.startedAt ?? undefined;
    message.expiresAt = object.expiresAt ?? undefined;
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseProviderBondRecord(): ProviderBondRecord {
  return { provider: "", bondedAmount: undefined, unbondingEntries: [], slashed: false, lastSlashTime: undefined };
}

export const ProviderBondRecord: MessageFns<ProviderBondRecord, "akash.verification.v1.ProviderBondRecord"> = {
  $type: "akash.verification.v1.ProviderBondRecord" as const,

  encode(message: ProviderBondRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.bondedAmount !== undefined) {
      Coin.encode(message.bondedAmount, writer.uint32(18).fork()).join();
    }
    for (const v of message.unbondingEntries) {
      UnbondingEntry.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.slashed !== false) {
      writer.uint32(32).bool(message.slashed);
    }
    if (message.lastSlashTime !== undefined) {
      Timestamp.encode(toTimestamp(message.lastSlashTime), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderBondRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderBondRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bondedAmount = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.unbondingEntries.push(UnbondingEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.slashed = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lastSlashTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderBondRecord {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      bondedAmount: isSet(object.bonded_amount) ? Coin.fromJSON(object.bonded_amount) : undefined,
      unbondingEntries: globalThis.Array.isArray(object?.unbonding_entries)
        ? object.unbonding_entries.map((e: any) => UnbondingEntry.fromJSON(e))
        : [],
      slashed: isSet(object.slashed) ? globalThis.Boolean(object.slashed) : false,
      lastSlashTime: isSet(object.last_slash_time) ? fromJsonTimestamp(object.last_slash_time) : undefined,
    };
  },

  toJSON(message: ProviderBondRecord): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.bondedAmount !== undefined) {
      obj.bonded_amount = Coin.toJSON(message.bondedAmount);
    }
    if (message.unbondingEntries?.length) {
      obj.unbonding_entries = message.unbondingEntries.map((e) => UnbondingEntry.toJSON(e));
    }
    if (message.slashed !== false) {
      obj.slashed = message.slashed;
    }
    if (message.lastSlashTime !== undefined) {
      obj.last_slash_time = message.lastSlashTime.toISOString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<ProviderBondRecord>): ProviderBondRecord {
    const message = createBaseProviderBondRecord();
    message.provider = object.provider ?? "";
    message.bondedAmount = (object.bondedAmount !== undefined && object.bondedAmount !== null)
      ? Coin.fromPartial(object.bondedAmount)
      : undefined;
    message.unbondingEntries = object.unbondingEntries?.map((e) => UnbondingEntry.fromPartial(e)) || [];
    message.slashed = object.slashed ?? false;
    message.lastSlashTime = object.lastSlashTime ?? undefined;
    return message;
  },
};

function createBaseUnbondingEntry(): UnbondingEntry {
  return { amount: undefined, completionTime: undefined };
}

export const UnbondingEntry: MessageFns<UnbondingEntry, "akash.verification.v1.UnbondingEntry"> = {
  $type: "akash.verification.v1.UnbondingEntry" as const,

  encode(message: UnbondingEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(10).fork()).join();
    }
    if (message.completionTime !== undefined) {
      Timestamp.encode(toTimestamp(message.completionTime), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnbondingEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnbondingEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.completionTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnbondingEntry {
    return {
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
      completionTime: isSet(object.completion_time) ? fromJsonTimestamp(object.completion_time) : undefined,
    };
  },

  toJSON(message: UnbondingEntry): unknown {
    const obj: any = {};
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    if (message.completionTime !== undefined) {
      obj.completion_time = message.completionTime.toISOString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<UnbondingEntry>): UnbondingEntry {
    const message = createBaseUnbondingEntry();
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    message.completionTime = object.completionTime ?? undefined;
    return message;
  },
};

function createBaseSoftwareIdentity(): SoftwareIdentity {
  return {
    version: "",
    artifactRef: "",
    digestAlgorithm: "",
    digest: new Uint8Array(0),
    signatureType: "",
    signature: new Uint8Array(0),
    signatureRef: "",
    publicKeyRef: "",
  };
}

export const SoftwareIdentity: MessageFns<SoftwareIdentity, "akash.verification.v1.SoftwareIdentity"> = {
  $type: "akash.verification.v1.SoftwareIdentity" as const,

  encode(message: SoftwareIdentity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.artifactRef !== "") {
      writer.uint32(18).string(message.artifactRef);
    }
    if (message.digestAlgorithm !== "") {
      writer.uint32(26).string(message.digestAlgorithm);
    }
    if (message.digest.length !== 0) {
      writer.uint32(34).bytes(message.digest);
    }
    if (message.signatureType !== "") {
      writer.uint32(42).string(message.signatureType);
    }
    if (message.signature.length !== 0) {
      writer.uint32(50).bytes(message.signature);
    }
    if (message.signatureRef !== "") {
      writer.uint32(58).string(message.signatureRef);
    }
    if (message.publicKeyRef !== "") {
      writer.uint32(66).string(message.publicKeyRef);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SoftwareIdentity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSoftwareIdentity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.artifactRef = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.digestAlgorithm = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.digest = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.signatureType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.signatureRef = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.publicKeyRef = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SoftwareIdentity {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      artifactRef: isSet(object.artifact_ref) ? globalThis.String(object.artifact_ref) : "",
      digestAlgorithm: isSet(object.digest_algorithm) ? globalThis.String(object.digest_algorithm) : "",
      digest: isSet(object.digest) ? bytesFromBase64(object.digest) : new Uint8Array(0),
      signatureType: isSet(object.signature_type) ? globalThis.String(object.signature_type) : "",
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      signatureRef: isSet(object.signature_ref) ? globalThis.String(object.signature_ref) : "",
      publicKeyRef: isSet(object.public_key_ref) ? globalThis.String(object.public_key_ref) : "",
    };
  },

  toJSON(message: SoftwareIdentity): unknown {
    const obj: any = {};
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.artifactRef !== "") {
      obj.artifact_ref = message.artifactRef;
    }
    if (message.digestAlgorithm !== "") {
      obj.digest_algorithm = message.digestAlgorithm;
    }
    if (message.digest.length !== 0) {
      obj.digest = base64FromBytes(message.digest);
    }
    if (message.signatureType !== "") {
      obj.signature_type = message.signatureType;
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.signatureRef !== "") {
      obj.signature_ref = message.signatureRef;
    }
    if (message.publicKeyRef !== "") {
      obj.public_key_ref = message.publicKeyRef;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<SoftwareIdentity>): SoftwareIdentity {
    const message = createBaseSoftwareIdentity();
    message.version = object.version ?? "";
    message.artifactRef = object.artifactRef ?? "";
    message.digestAlgorithm = object.digestAlgorithm ?? "";
    message.digest = object.digest ?? new Uint8Array(0);
    message.signatureType = object.signatureType ?? "";
    message.signature = object.signature ?? new Uint8Array(0);
    message.signatureRef = object.signatureRef ?? "";
    message.publicKeyRef = object.publicKeyRef ?? "";
    return message;
  },
};

function createBaseResourceSummary(): ResourceSummary {
  return {
    totalGpus: 0,
    totalVcpus: 0,
    totalMemoryMb: Long.UZERO,
    totalStorageMb: Long.UZERO,
    activeLeases: 0,
    softwareVersion: "",
    softwareSignature: new Uint8Array(0),
    softwareIdentity: undefined,
  };
}

export const ResourceSummary: MessageFns<ResourceSummary, "akash.verification.v1.ResourceSummary"> = {
  $type: "akash.verification.v1.ResourceSummary" as const,

  encode(message: ResourceSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalGpus !== 0) {
      writer.uint32(8).uint32(message.totalGpus);
    }
    if (message.totalVcpus !== 0) {
      writer.uint32(16).uint32(message.totalVcpus);
    }
    if (!message.totalMemoryMb.equals(Long.UZERO)) {
      writer.uint32(24).uint64(message.totalMemoryMb.toString());
    }
    if (!message.totalStorageMb.equals(Long.UZERO)) {
      writer.uint32(32).uint64(message.totalStorageMb.toString());
    }
    if (message.activeLeases !== 0) {
      writer.uint32(40).uint32(message.activeLeases);
    }
    if (message.softwareVersion !== "") {
      writer.uint32(50).string(message.softwareVersion);
    }
    if (message.softwareSignature.length !== 0) {
      writer.uint32(58).bytes(message.softwareSignature);
    }
    if (message.softwareIdentity !== undefined) {
      SoftwareIdentity.encode(message.softwareIdentity, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResourceSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResourceSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.totalGpus = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalVcpus = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalMemoryMb = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalStorageMb = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.activeLeases = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.softwareVersion = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.softwareSignature = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.softwareIdentity = SoftwareIdentity.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResourceSummary {
    return {
      totalGpus: isSet(object.total_gpus) ? globalThis.Number(object.total_gpus) : 0,
      totalVcpus: isSet(object.total_vcpus) ? globalThis.Number(object.total_vcpus) : 0,
      totalMemoryMb: isSet(object.total_memory_mb) ? Long.fromValue(object.total_memory_mb) : Long.UZERO,
      totalStorageMb: isSet(object.total_storage_mb) ? Long.fromValue(object.total_storage_mb) : Long.UZERO,
      activeLeases: isSet(object.active_leases) ? globalThis.Number(object.active_leases) : 0,
      softwareVersion: isSet(object.software_version) ? globalThis.String(object.software_version) : "",
      softwareSignature: isSet(object.software_signature)
        ? bytesFromBase64(object.software_signature)
        : new Uint8Array(0),
      softwareIdentity: isSet(object.software_identity)
        ? SoftwareIdentity.fromJSON(object.software_identity)
        : undefined,
    };
  },

  toJSON(message: ResourceSummary): unknown {
    const obj: any = {};
    if (message.totalGpus !== 0) {
      obj.total_gpus = Math.round(message.totalGpus);
    }
    if (message.totalVcpus !== 0) {
      obj.total_vcpus = Math.round(message.totalVcpus);
    }
    if (!message.totalMemoryMb.equals(Long.UZERO)) {
      obj.total_memory_mb = (message.totalMemoryMb || Long.UZERO).toString();
    }
    if (!message.totalStorageMb.equals(Long.UZERO)) {
      obj.total_storage_mb = (message.totalStorageMb || Long.UZERO).toString();
    }
    if (message.activeLeases !== 0) {
      obj.active_leases = Math.round(message.activeLeases);
    }
    if (message.softwareVersion !== "") {
      obj.software_version = message.softwareVersion;
    }
    if (message.softwareSignature.length !== 0) {
      obj.software_signature = base64FromBytes(message.softwareSignature);
    }
    if (message.softwareIdentity !== undefined) {
      obj.software_identity = SoftwareIdentity.toJSON(message.softwareIdentity);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<ResourceSummary>): ResourceSummary {
    const message = createBaseResourceSummary();
    message.totalGpus = object.totalGpus ?? 0;
    message.totalVcpus = object.totalVcpus ?? 0;
    message.totalMemoryMb = (object.totalMemoryMb !== undefined && object.totalMemoryMb !== null)
      ? Long.fromValue(object.totalMemoryMb)
      : Long.UZERO;
    message.totalStorageMb = (object.totalStorageMb !== undefined && object.totalStorageMb !== null)
      ? Long.fromValue(object.totalStorageMb)
      : Long.UZERO;
    message.activeLeases = object.activeLeases ?? 0;
    message.softwareVersion = object.softwareVersion ?? "";
    message.softwareSignature = object.softwareSignature ?? new Uint8Array(0);
    message.softwareIdentity = (object.softwareIdentity !== undefined && object.softwareIdentity !== null)
      ? SoftwareIdentity.fromPartial(object.softwareIdentity)
      : undefined;
    return message;
  },
};

function createBaseProviderSnapshotRecord(): ProviderSnapshotRecord {
  return {
    provider: "",
    snapshotHash: new Uint8Array(0),
    resourceSummary: undefined,
    postedAt: undefined,
    snapshotTimestamp: undefined,
    complianceDeadline: undefined,
    suspended: false,
  };
}

export const ProviderSnapshotRecord: MessageFns<
  ProviderSnapshotRecord,
  "akash.verification.v1.ProviderSnapshotRecord"
> = {
  $type: "akash.verification.v1.ProviderSnapshotRecord" as const,

  encode(message: ProviderSnapshotRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.snapshotHash.length !== 0) {
      writer.uint32(18).bytes(message.snapshotHash);
    }
    if (message.resourceSummary !== undefined) {
      ResourceSummary.encode(message.resourceSummary, writer.uint32(26).fork()).join();
    }
    if (message.postedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.postedAt), writer.uint32(34).fork()).join();
    }
    if (message.snapshotTimestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.snapshotTimestamp), writer.uint32(42).fork()).join();
    }
    if (message.complianceDeadline !== undefined) {
      Timestamp.encode(toTimestamp(message.complianceDeadline), writer.uint32(50).fork()).join();
    }
    if (message.suspended !== false) {
      writer.uint32(56).bool(message.suspended);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderSnapshotRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderSnapshotRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.snapshotHash = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resourceSummary = ResourceSummary.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.postedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.snapshotTimestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.complianceDeadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.suspended = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderSnapshotRecord {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      snapshotHash: isSet(object.snapshot_hash) ? bytesFromBase64(object.snapshot_hash) : new Uint8Array(0),
      resourceSummary: isSet(object.resource_summary) ? ResourceSummary.fromJSON(object.resource_summary) : undefined,
      postedAt: isSet(object.posted_at) ? fromJsonTimestamp(object.posted_at) : undefined,
      snapshotTimestamp: isSet(object.snapshot_timestamp) ? fromJsonTimestamp(object.snapshot_timestamp) : undefined,
      complianceDeadline: isSet(object.compliance_deadline) ? fromJsonTimestamp(object.compliance_deadline) : undefined,
      suspended: isSet(object.suspended) ? globalThis.Boolean(object.suspended) : false,
    };
  },

  toJSON(message: ProviderSnapshotRecord): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.snapshotHash.length !== 0) {
      obj.snapshot_hash = base64FromBytes(message.snapshotHash);
    }
    if (message.resourceSummary !== undefined) {
      obj.resource_summary = ResourceSummary.toJSON(message.resourceSummary);
    }
    if (message.postedAt !== undefined) {
      obj.posted_at = message.postedAt.toISOString();
    }
    if (message.snapshotTimestamp !== undefined) {
      obj.snapshot_timestamp = message.snapshotTimestamp.toISOString();
    }
    if (message.complianceDeadline !== undefined) {
      obj.compliance_deadline = message.complianceDeadline.toISOString();
    }
    if (message.suspended !== false) {
      obj.suspended = message.suspended;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<ProviderSnapshotRecord>): ProviderSnapshotRecord {
    const message = createBaseProviderSnapshotRecord();
    message.provider = object.provider ?? "";
    message.snapshotHash = object.snapshotHash ?? new Uint8Array(0);
    message.resourceSummary = (object.resourceSummary !== undefined && object.resourceSummary !== null)
      ? ResourceSummary.fromPartial(object.resourceSummary)
      : undefined;
    message.postedAt = object.postedAt ?? undefined;
    message.snapshotTimestamp = object.snapshotTimestamp ?? undefined;
    message.complianceDeadline = object.complianceDeadline ?? undefined;
    message.suspended = object.suspended ?? false;
    return message;
  },
};

function _unused_bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function _unused_base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function _unused_fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function _unused_fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function _unused_numberToLong(number: number) {
  return Long.fromNumber(number);
}

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
